# Шаблон CTF Write-up

> Полный шаблон для написания write-up по CTF заданиям

---

## Как пользоваться шаблоном

1. Скопируй нужный шаблон ниже
2. Заполни секции своими данными
3. Замени `[текст в скобках]` на реальные данные
4. Удали секции которые не применимы
5. Добавь скриншоты там где указано `[SCREENSHOT]`

---

## Полный шаблон Write-up

```markdown
# [CTF Name] [Год] — [Challenge Name] Write-up

---

## Информация о задании

| Параметр | Значение |
|----------|----------|
| **CTF** | [Название CTF, например: CTFtime.org/event/1234] |
| **Дата** | [DD.MM.YYYY] |
| **Категория** | [Web / Crypto / Forensics / Pwn / Reverse / OSINT / Misc] |
| **Название** | [Challenge Name] |
| **Сложность** | [Easy / Medium / Hard] |
| **Очки** | [250 / 500 pts] |
| **Решили** | [47 teams] |
| **Автор задания** | [@author_handle] |

---

## Описание задания

> *Оригинальный текст задания (цитата)*

```
[Дополнительная информация из задания:
URL: http://challenge.ctf.example.com:5000/
File: challenge.zip
Hint: "Something is hiding in plain sight"]
```

---

## Анализ задания

### Первый взгляд

[Опиши что ты видишь при первом взгляде на задание.
Что сразу бросается в глаза? Какие есть подсказки в описании?]

[SCREENSHOT: Главная страница/файл задания]

### Используемые технологии

- [Язык: PHP / Python / Node.js]
- [Фреймворк: Laravel / Flask / Express]
- [База данных: MySQL / PostgreSQL / SQLite]
- [Другое: JWT, Jinja2, etc.]

---

## Решение

### Шаг 1: [Название шага]

[Подробное описание первого шага. Что ты делал и почему.]

```bash
# Команды которые ты выполнял
curl -v http://challenge.ctf.example.com:5000/
```

[Что ты получил в ответ и что это значит:]

```
HTTP/1.1 200 OK
Server: Apache/2.4.49
X-Powered-By: PHP/7.4.0

[Ответ сервера]
```

[SCREENSHOT: Результат первого шага]

---

### Шаг 2: [Название шага]

[Описание второго шага. Почему ты пошёл именно этим путём?
Что навело тебя на эту мысль?]

```python
#!/usr/bin/env python3
# Скрипт для шага 2
import requests

TARGET = "http://challenge.ctf.example.com:5000"

payload = "[твой payload]"
resp = requests.get(f"{TARGET}/vulnerable?param={payload}")
print(resp.text)
```

**Объяснение payload:**

[КРИТИЧЕСКИ ВАЖНО: объясни почему именно этот payload работает.
Не просто "я попробовал и сработало", а "это работает потому что..."]

Технический разбор:
- `[часть payload]` — [что это делает и почему]
- `[следующая часть]` — [объяснение]

[SCREENSHOT: Результат выполнения]

---

### Шаг 3: [Получение флага]

[Последний шаг. Как ты получил флаг.]

```bash
# Итоговая команда/запрос
curl "http://challenge.ctf.example.com:5000/exploit?payload=FINAL_PAYLOAD"
```

[SCREENSHOT: Флаг на экране]

---

## Флаг

```
CTF{[YOUR_FLAG_HERE]}
```

---

## Неудачные попытки (необязательно, но ценно!)

[Опиши что ты пробовал и почему не сработало. Это очень полезно для читателей!]

### Попытка 1: [Что пробовал]

```
[Что делал]
```

**Почему не сработало:** [Объяснение]

### Попытка 2: [Что пробовал]

**Почему не сработало:** [Объяснение]

---

## Ключевые выводы (Key Takeaways)

- [Что нового ты узнал из этого задания]
- [Какую технику или концепцию ты освоил]
- [Что можно применить в реальном пентесте/Bug Bounty]

---

## Использованные инструменты

| Инструмент | Версия | Для чего |
|-----------|--------|----------|
| Burp Suite | 2024.x | Перехват и анализ запросов |
| Python requests | 2.31.0 | Автоматизация эксплойта |
| ffuf | v2.1.0 | Directory fuzzing |
| [другое] | [версия] | [для чего] |

---

## Защита (как это исправить)

[Объясни как организация/разработчик может исправить эту уязвимость.
Это делает write-up более ценным и профессиональным.]

**Корневая причина:** [Описание уязвимости]

**Рекомендации:**
1. [Первая рекомендация]
2. [Вторая рекомендация]
3. [Конкретный код/конфигурация исправления]

```php
// Уязвимый код:
$query = "SELECT * FROM users WHERE id = " . $_GET['id'];

// Исправленный код:
$stmt = $pdo->prepare("SELECT * FROM users WHERE id = ?");
$stmt->execute([$_GET['id']]);
```

---

## Ссылки

- [Ссылка на задание/архив CTF]
- [Статья/документация по использованной технике]
- [CVE если применимо]
- [Другие write-ups по той же уязвимости для сравнения]
```

---

## Короткий шаблон (для простых заданий)

```markdown
# [CTF] — [Challenge] | [Category] [Points]

## TL;DR
[Одно предложение: что это и как решил]

## Описание
> [цитата задания]

## Решение

[Краткое описание что делал]

```bash
# Основные команды
```

**Объяснение:** [Почему это сработало]

## Флаг
`CTF{flag_here}`
```

---

## Специализированные шаблоны по категориям

### Web Challenge шаблон

```markdown
# [CTF] — [Challenge] | Web | [Points]

## Описание
> [цитата]

## Разведка (Reconnaissance)

### Ручной анализ
- Открыт сайт, вижу: [что видишь]
- Source code (Ctrl+U): [что нашёл в HTML]
- Network tab: [интересные запросы]
- Cookies: [что в cookies]

### Directory Fuzzing
```bash
ffuf -u http://target/FUZZ -w wordlist.txt -mc 200,301
```

Найдено: [что нашёл]

### Анализ технологий
- Server: [Apache/nginx версия из заголовков]
- X-Powered-By: [PHP/Node версия]
- Cookies: [название, тип — JWT/base64/etc]

## Идентификация уязвимости

[Как определил тип уязвимости. Какие признаки?]

```
# Пример тестирования
curl "http://target/page?param=test'"
```

Ответ: [что вернул сервер, почему это говорит об уязвимости]

## Эксплуатация

```python
# Exploit script
import requests

TARGET = "http://target"
payload = "[финальный payload]"

resp = requests.get(TARGET, params={"param": payload})
print(resp.text)
```

**Разбор payload:**
```
[payload разбит по частям]
├── [часть 1]: [что делает]
├── [часть 2]: [что делает]
└── [часть 3]: [что делает]
```

## Флаг
`CTF{...}`

## Уязвимость
[SQLi / XSS / SSRF / LFI / SSTI / JWT / etc]

**Класс:** [OWASP категория]
**CVSS:** [оценка если применимо]
```

### Forensics Challenge шаблон

```markdown
# [CTF] — [Challenge] | Forensics | [Points]

## Описание
> [цитата]

## Анализ файла

```bash
# Определение типа файла
file challenge.bin
xxd challenge.bin | head -20
strings challenge.bin | head -30
```

Тип файла: [что определил]

## Временная линия событий (если PCAP/дамп памяти)

| Время | Событие | Источник |
|-------|---------|----------|
| 10:23 | [событие] | [откуда] |

## Инструменты использованные

- Wireshark: [что делал]
- Volatility: [если RAM дамп]
- Binwalk: [если стеганография]
- steghide: [если стеганография]

## Процесс решения

[Пошаговое описание]

```bash
# Команды с результатами
volatility -f memory.raw --profile=Win10x64 pslist
```

## Флаг
`CTF{...}`
```

### Crypto Challenge шаблон

```markdown
# [CTF] — [Challenge] | Crypto | [Points]

## Описание
> [цитата]

## Анализ шифрования

**Тип:** [RSA / AES / XOR / Vigenere / Custom]
**Ключевые данные:**
- n = [значение если RSA]
- e = [значение]
- Ciphertext: [зашифрованный текст]

## Уязвимость

[Почему это можно взломать? Слабые параметры, известная атака?]

Атака: [Small prime / Wiener / LSB oracle / etc]

## Решение

```python
# Скрипт взлома
from Crypto.PublicKey import RSA
from Crypto.Util.number import *

n = [значение]
e = [значение]
c = [ciphertext]

# [Твой метод]
p, q = factorize(n)  # или другой метод
phi = (p-1) * (q-1)
d = inverse(e, phi)
m = pow(c, d, n)
print(long_to_bytes(m))
```

## Флаг
`CTF{...}`

## Математика (объяснение)

[Объясни математическую основу атаки. Это делает write-up ценным для обучения.]
```

---

## Советы по написанию хороших write-up

### Структура

```
ХОРОШИЙ WRITE-UP ВКЛЮЧАЕТ:

✅ Заголовок с категорией и очками
✅ Ссылки на источники
✅ Описание исходной точки (что видишь сразу)
✅ Описание процесса размышления (не только решение)
✅ Неудачные попытки (ценный опыт!)
✅ Объяснение payload — ПОЧЕМУ это работает
✅ Скриншоты / вывод команд
✅ Рекомендации по защите
✅ Использованные инструменты с версиями

❌ ПЛОХОЙ WRITE-UP:
- Только итоговый payload без объяснений
- "Я запустил sqlmap и получил флаг"
- Нет объяснения почему уязвимость существует
- Нет процесса решения (только ответ)
- Опубликован во время CTF (СПОЙЛЕР!)
```

### Примеры хорошего объяснения payload

```
ПЛОХО:
"Я использовал payload: {{7*7}} и получил 49, значит SSTI"

ХОРОШО:
"Введя {{7*7}} в поле имени и получив ответ '49' (а не '{{7*7}}'),
я подтвердил что шаблонизатор интерпретирует выражения.
Результат '49' (а не '7*7' или ошибка) указывает на Jinja2/Twig.

В Jinja2 двойные фигурные скобки {{ }} обозначают выражение.
Выражение 7*7 вычисляется и результат (49) подставляется в HTML.
Это означает что я могу выполнять произвольный Python код через шаблонизатор.

Следующий payload для RCE:
{{''.__class__.__bases__[0].__subclasses__()[258](['cat','/flag.txt'],stdout=-1).communicate()[0]}}

Разбор:
- ''.__class__    → тип str
- .__bases__[0]  → базовый класс object
- .__subclasses__() → все подклассы (включая Popen)
- [258]          → subprocess.Popen (индекс может отличаться)
- (['cmd'],stdout=-1) → выполнить команду, захватить вывод
- .communicate()[0] → получить вывод"
```

### Где публиковать write-up

```
ПЛАТФОРМЫ ДЛЯ ПУБЛИКАЦИИ:

Personal blog (лучший вариант):
└── GitHub Pages (бесплатно, markdown)
└── Medium (широкая аудитория)
└── Hashnode (технический блог)

CTF-специфичные:
└── CTFtime.org (можно добавить ссылку)
└── pwn.college writeups section

GitHub:
└── Репозиторий с write-ups (хорошо для портфолио)
└── Пример: github.com/username/ctf-writeups

ПРАВИЛА ПУБЛИКАЦИИ:
✓ Публикуй ПОСЛЕ окончания CTF (обычно 24-48 часов)
✓ Всегда указывай название CTF и дату
✓ Давай credit другим авторам если использовал их идеи
✓ Не публикуй флаги активных CTF
```

---

## Быстрые сниппеты для write-up

### Вставка скриншота

```markdown
![Описание скриншота](./screenshots/step1.png)
*Рисунок 1: Страница входа с уязвимым полем поиска*
```

### Вставка HTTP запроса/ответа

```markdown
**Запрос:**
```http
GET /page?id=1 HTTP/1.1
Host: challenge.ctf.com
Cookie: session=eyJ...
```

**Ответ:**
```http
HTTP/1.1 200 OK
Content-Type: text/html

<html>...флаг здесь...</html>
```
```

### Вставка Burp Suite сниппета

```markdown
**Перехваченный запрос (Burp Suite):**
```
POST /login HTTP/1.1
Host: target.com
Content-Type: application/x-www-form-urlencoded

username=admin'--&password=test
```

Изменил `username` на `admin'--` — SQL инъекция в поле пользователя.
```

### Временная линия решения

```markdown
## Временная линия (для сложных заданий)

```
00:00 — Прочитал описание, открыл URL
00:10 — Нашёл robots.txt, /backup директорию
00:25 — Обнаружил загрузку файлов, пробую обойти фильтр
00:45 — Загружен PHP webshell, есть RCE
00:50 — Прочитал /flag.txt, получен флаг
```
```

---

## Словарь CTF терминов

| Термин | Описание |
|--------|----------|
| **Flag** | Строка в формате CTF{...} — цель задания |
| **Writeup** | Технический разбор решения задания |
| **Payload** | Вредоносная нагрузка для эксплойта |
| **PoC** | Proof of Concept — демонстрация уязвимости |
| **Jeopardy** | Формат CTF: набор независимых заданий |
| **A/D** | Attack-Defense: атакуй чужих, защищай своих |
| **KotH** | King of the Hill: захват и удержание флага |
| **Pwn** | Успешная эксплуатация (взломать) |
| **Rev** | Reverse Engineering |
| **Chall** | Challenge (задание) |
| **OOB** | Out-of-Band (через сторонний канал) |
| **Blind** | Уязвимость без видимого вывода |
| **RCE** | Remote Code Execution |
| **LPE** | Local Privilege Escalation |
| **IDOR** | Insecure Direct Object Reference |
| **SSRF** | Server-Side Request Forgery |
| **SSTI** | Server-Side Template Injection |
| **XSS** | Cross-Site Scripting |
| **SQLi** | SQL Injection |
| **LFI** | Local File Inclusion |
| **Regex** | Regular Expression |
| **WAF** | Web Application Firewall |
| **Bypass** | Обход защитного механизма |
| **Enumerate** | Перечисление (поиск всех возможных значений) |
| **Fuzz** | Автоматизированный перебор |
| **Dork** | Специальный поисковый запрос |
| **IOC** | Indicator of Compromise |
| **OSINT** | Open Source Intelligence |
