# Глава 1.1: Модель OSI и стек TCP/IP

## 🎯 Цели главы

- Понять назначение и структуру модели OSI (все 7 уровней)
- Сравнить модель OSI с практической моделью TCP/IP
- Освоить IP-адресацию: классы, маски подсетей, нотацию CIDR
- Разобраться в разнице между TCP и UDP и понять, когда применять каждый протокол
- Детально изучить трёхстороннее рукопожатие TCP
- Научиться использовать инструменты ping и traceroute для диагностики сети

---

## 1.1.1 Модель OSI: семь уровней сети

Модель OSI (Open Systems Interconnection) — эталонная модель, описывающая, как данные передаются по сети. Она была разработана ISO в 1984 году и состоит из 7 уровней. Каждый уровень выполняет строго определённую функцию и взаимодействует только с соседними уровнями.

> Важно: модель OSI — это концептуальная основа. В реальных сетях используется стек TCP/IP, но понимание OSI критически важно для диагностики проблем и анализа атак.

### Уровень 7 — Прикладной (Application Layer)

Самый верхний уровень. Обеспечивает интерфейс между сетью и приложениями пользователя.

**Протоколы:** HTTP, HTTPS, FTP, SMTP, POP3, IMAP, DNS, SSH, Telnet, SNMP, RDP, SIP

**Примеры:**
- Браузер отправляет HTTP GET-запрос к веб-серверу
- Почтовый клиент использует SMTP для отправки письма
- SSH-клиент устанавливает защищённое соединение с сервером

**Атаки на этом уровне:** SQL-инъекции, XSS, CSRF, XXE, directory traversal, фишинг

---

### Уровень 6 — Представительский (Presentation Layer)

Отвечает за форматирование, шифрование и сжатие данных. Гарантирует, что данные, отправленные одним приложением, могут быть прочитаны другим.

**Функции:**
- Кодирование: ASCII, Unicode, EBCDIC
- Шифрование/дешифрование: SSL/TLS (формально на этом уровне)
- Сжатие: gzip, deflate

**Форматы данных:** JPEG, PNG, MP4, PDF, JSON, XML

**Примеры:**
- Браузер получает gzip-сжатый HTML и распаковывает его
- TLS-шифрование трафика HTTPS
- Конвертация кодировок при передаче текста

---

### Уровень 5 — Сеансовый (Session Layer)

Управляет установкой, поддержанием и завершением сеансов связи между приложениями.

**Функции:**
- Управление диалогом (simplex, half-duplex, full-duplex)
- Синхронизация и контрольные точки
- Восстановление соединения

**Протоколы:** NetBIOS, RPC, SQL sessions, NFS, SMB (сеансовая часть)

**Примеры:**
- Видеоконференция поддерживает длительный сеанс
- При обрыве FTP-передачи файла можно возобновить с контрольной точки
- Аутентификация в Kerberos управляет сеансами

---

### Уровень 4 — Транспортный (Transport Layer)

Обеспечивает надёжную (или ненадёжную) передачу данных между приложениями. Вводит понятие **портов**.

**Протоколы:** TCP, UDP, SCTP

**Ключевые функции TCP:**
- Нумерация сегментов и подтверждение (ACK)
- Контроль потока (sliding window)
- Контроль перегрузки
- Гарантия доставки и порядка

**Ключевые функции UDP:**
- Отправка датаграмм без подтверждения
- Минимальные накладные расходы
- Не гарантирует порядок или доставку

**Атаки:** SYN flood, UDP flood, port scanning

---

### Уровень 3 — Сетевой (Network Layer)

Отвечает за логическую адресацию и маршрутизацию пакетов между сетями.

**Протоколы:** IP (IPv4, IPv6), ICMP, OSPF, BGP, RIP, ARP (спорно — часто относят к L2/L3)

**Ключевые функции:**
- IP-адресация (логические адреса)
- Маршрутизация пакетов через множество сетей
- Фрагментация и сборка пакетов
- TTL (Time To Live) — предотвращение бесконечной маршрутизации

**Устройства:** Маршрутизаторы (routers), L3-коммутаторы

**Атаки:** IP spoofing, ICMP flood (ping flood), route hijacking

---

### Уровень 2 — Канальный (Data Link Layer)

Обеспечивает передачу данных между непосредственно соединёнными узлами. Использует **MAC-адреса**.

**Подуровни:**
- LLC (Logical Link Control) — управление логической связью
- MAC (Media Access Control) — управление доступом к среде

**Протоколы:** Ethernet (802.3), Wi-Fi (802.11), PPP, HDLC, ARP (часто здесь)

**Функции:**
- Формирование фреймов (кадров)
- Адресация по MAC
- Обнаружение ошибок (CRC)
- Управление доступом к среде (CSMA/CD)

**Устройства:** Коммутаторы (switches), точки доступа Wi-Fi, сетевые карты

**Атаки:** ARP spoofing, MAC flooding, VLAN hopping

---

### Уровень 1 — Физический (Physical Layer)

Передача «сырых» битов по физической среде.

**Функции:**
- Определение физических характеристик: напряжение, частота, тип разъёма
- Кодирование битов в сигналы
- Синхронизация битов

**Среды передачи:** Витая пара (UTP/STP), коаксиальный кабель, оптоволокно, радиоволны (Wi-Fi, Bluetooth)

**Стандарты:** RS-232, USB, Ethernet (физический уровень 10BASE-T, 100BASE-TX)

**Устройства:** Кабели, концентраторы (hubs), повторители (repeaters), медиаконверторы

**Атаки:** Физическое прослушивание (wiretapping), помехи (jamming)

---

### Мнемоника для запоминания уровней OSI

**Сверху вниз (7→1):** «Пожалуйста, Предоставьте Специалисту Технику Новый Коммутатор Проводов»
- **П**рикладной
- **П**редставительский
- **С**еансовый
- **Т**ранспортный
- **С**етевой (Network)
- **К**анальный
- **Ф**изический

**Снизу вверх (1→7):** «Физики Кидают Сети Тоннами Спасательного Прикладного Программирования»

---

## 1.1.2 Стек TCP/IP: практическая модель

Модель TCP/IP (также называемая DoD-моделью или Internet-моделью) — это практическая реализация сетевого взаимодействия, используемая в интернете. В отличие от OSI с её 7 уровнями, TCP/IP использует 4 уровня.

### Сравнение OSI и TCP/IP

```
OSI (7 уровней)          TCP/IP (4 уровня)        Примеры протоколов
─────────────────────────────────────────────────────────────────────
7. Прикладной            ┐
6. Представительский     ├── Прикладной            HTTP, HTTPS, DNS,
5. Сеансовый             ┘                         FTP, SMTP, SSH
─────────────────────────────────────────────────────────────────────
4. Транспортный          ─── Транспортный           TCP, UDP
─────────────────────────────────────────────────────────────────────
3. Сетевой               ─── Интернет               IP, ICMP, ARP
─────────────────────────────────────────────────────────────────────
2. Канальный             ┐
1. Физический            ┴── Сетевого доступа       Ethernet, Wi-Fi
─────────────────────────────────────────────────────────────────────
```

### Инкапсуляция данных

При отправке данные проходят через уровни сверху вниз, каждый уровень добавляет свой заголовок:

```
Приложение:   [Данные]
Транспортный: [TCP-заголовок | Данные]           → Сегмент
Сетевой:      [IP-заголовок | TCP-заголовок | Данные]  → Пакет
Канальный:    [MAC-заголовок | Пакет | FCS]      → Фрейм
Физический:   биты: 01001010 10110101...
```

При получении происходит обратный процесс — деинкапсуляция.

---

## 1.1.3 IP-адресация

### Структура IPv4-адреса

IPv4-адрес — это 32-битное число, записываемое в виде четырёх октетов (байт), разделённых точками:

```
192.168.1.100

В двоичном виде:
11000000 . 10101000 . 00000001 . 01100100
   192   .    168   .    1     .   100
```

Каждый октет имеет значение от 0 до 255.

### Классы IP-адресов (классовая адресация)

Исторически IPv4-адреса делились на классы:

```
Класс A: 1.0.0.0   – 126.255.255.255  Маска: 255.0.0.0     (/8)
         Сети: 126, Хостов: 16,777,214
         Используется: крупные организации

Класс B: 128.0.0.0 – 191.255.255.255  Маска: 255.255.0.0   (/16)
         Сети: 16,384, Хостов: 65,534
         Используется: средние организации

Класс C: 192.0.0.0 – 223.255.255.255  Маска: 255.255.255.0 (/24)
         Сети: 2,097,152, Хостов: 254
         Используется: малые организации

Класс D: 224.0.0.0 – 239.255.255.255  Многоадресная рассылка (multicast)

Класс E: 240.0.0.0 – 255.255.255.255  Зарезервировано
```

### Частные IP-адреса (RFC 1918)

Эти диапазоны не маршрутизируются в интернете:

```
10.0.0.0     – 10.255.255.255   (10.0.0.0/8)      — класс A
172.16.0.0   – 172.31.255.255   (172.16.0.0/12)   — класс B
192.168.0.0  – 192.168.255.255  (192.168.0.0/16)  — класс C
```

**Специальные адреса:**
- `127.0.0.1` — loopback (localhost)
- `0.0.0.0` — текущая сеть или «любой адрес»
- `255.255.255.255` — широковещательный (broadcast) для всей сети
- `169.254.0.0/16` — APIPA (автоматическая конфигурация при отсутствии DHCP)

### CIDR (Classless Inter-Domain Routing)

CIDR — бесклассовая адресация. Вместо фиксированных классов используется произвольная длина префикса:

```
192.168.1.0/24

/24 означает: первые 24 бита — сетевая часть, остальные 8 — хостовая
```

### Вычисление подсетей: пошаговые примеры

**Пример 1:** Дано: `192.168.1.0/24`

```
IP-адрес:          192.168.1.0
Маска (/24):       255.255.255.0
                   11111111.11111111.11111111.00000000

Сетевой адрес:     192.168.1.0   (все хостовые биты = 0)
Broadcast:         192.168.1.255 (все хостовые биты = 1)
Диапазон хостов:   192.168.1.1 – 192.168.1.254
Количество хостов: 2^8 - 2 = 254
```

**Пример 2:** Дано: `172.16.10.0/20`

```
Маска (/20):       255.255.240.0
                   11111111.11111111.11110000.00000000

IP в двоичном:     10101100.00010000.00001010.00000000
                   172      .16      .10      .0

Сетевой адрес:     172.16.0.0    (обнуляем хостовые биты)
Broadcast:         172.16.15.255 (устанавливаем все хостовые биты в 1)
Диапазон хостов:   172.16.0.1 – 172.16.15.254
Количество хостов: 2^12 - 2 = 4094
```

**Пример 3 (деление сети):** Разделим `192.168.1.0/24` на 4 подсети:

```
Нужно 4 подсети → 2^2 = 4, значит берём 2 дополнительных бита
Новый префикс: /24 + 2 = /26
Маска: 255.255.255.192

Подсеть 1: 192.168.1.0/26    хосты: .1–.62    broadcast: .63
Подсеть 2: 192.168.1.64/26   хосты: .65–.126  broadcast: .127
Подсеть 3: 192.168.1.128/26  хосты: .129–.190 broadcast: .191
Подсеть 4: 192.168.1.192/26  хосты: .193–.254 broadcast: .255
```

### Таблица CIDR для быстрого справки

```
Префикс  Маска              Хостов    Подходит для
/30      255.255.255.252    2         Point-to-point соединения
/29      255.255.255.248    6         Малые сети
/28      255.255.255.240    14        Малые офисы
/27      255.255.255.224    30        Малые офисы
/26      255.255.255.192    62        Средние офисы
/25      255.255.255.128    126       Средние офисы
/24      255.255.255.0      254       Стандартная локальная сеть
/23      255.255.254.0      510       Средние организации
/22      255.255.252.0      1022      Средние организации
/16      255.255.0.0        65534     Крупные организации
/8       255.0.0.0          16777214  ISP/Крупные корпорации
```

---

## 1.1.4 TCP vs UDP

### TCP (Transmission Control Protocol)

TCP — протокол с установлением соединения, обеспечивающий надёжную доставку данных.

**Характеристики:**
- Ориентирован на соединение (connection-oriented)
- Гарантирует доставку всех данных
- Гарантирует порядок доставки
- Контроль потока и перегрузки
- Обнаружение и повторная передача потерянных пакетов
- Высокие накладные расходы (заголовок 20+ байт)

**Структура TCP-заголовка:**
```
Биты: 0        15 16      31
      ┌──────────┬──────────┐
      │ Порт src │ Порт dst │
      ├──────────┴──────────┤
      │  Sequence Number    │
      ├─────────────────────┤
      │  Acknowledgment Num │
      ├────┬────┬───────────┤
      │Data│Res │  Flags    │  ← SYN, ACK, FIN, RST, PSH, URG
      │Offs│    │           │
      ├────┴────┴───────────┤
      │ Window Size         │
      ├──────────┬──────────┤
      │ Checksum │ Urg Ptr  │
      └──────────┴──────────┘
```

**TCP флаги:**
- `SYN` (Synchronize) — инициация соединения, синхронизация ISN
- `ACK` (Acknowledgment) — подтверждение получения данных
- `FIN` (Finish) — завершение соединения
- `RST` (Reset) — сброс соединения (ошибка или принудительное закрытие)
- `PSH` (Push) — немедленная передача данных приложению (не буферизировать)
- `URG` (Urgent) — данные срочные, обработать вне очереди

**Когда использовать TCP:**
- Веб-серфинг (HTTP/HTTPS)
- Передача файлов (FTP, SFTP)
- Электронная почта (SMTP, IMAP, POP3)
- Удалённое управление (SSH, RDP, Telnet)
- Базы данных (MySQL, PostgreSQL)

---

### UDP (User Datagram Protocol)

UDP — протокол без установления соединения, обеспечивающий быструю, но ненадёжную доставку.

**Характеристики:**
- Без установления соединения (connectionless)
- Не гарантирует доставку
- Не гарантирует порядок
- Нет контроля потока
- Минимальные накладные расходы (заголовок 8 байт)
- Поддерживает broadcast и multicast

**Структура UDP-заголовка:**
```
Биты: 0        15 16      31
      ┌──────────┬──────────┐
      │ Порт src │ Порт dst │
      ├──────────┼──────────┤
      │  Length  │ Checksum │
      └──────────┴──────────┘
```

**Когда использовать UDP:**
- DNS-запросы (скорость важнее надёжности, повторить легко)
- Потоковое видео/аудио (лучше пропустить кадр, чем буферизировать)
- Онлайн-игры (задержка критичнее потерь)
- VoIP, видеоконференции
- DHCP (до установки IP-адреса)
- SNMP (мониторинг устройств)
- TFTP (простая передача файлов)

---

### Сравнение TCP и UDP

```
Характеристика      TCP                    UDP
────────────────────────────────────────────────────────
Соединение          Устанавливается         Не устанавливается
Надёжность          Гарантирована           Не гарантирована
Порядок данных      Гарантирован            Не гарантирован
Скорость            Медленнее               Быстрее
Заголовок           20-60 байт              8 байт
Контроль потока     Есть                    Нет
Применение          HTTP, SSH, FTP          DNS, VoIP, видео
```

---

## 1.1.5 Трёхстороннее рукопожатие TCP (Three-Way Handshake)

Прежде чем передавать данные, TCP устанавливает соединение с помощью трёхстороннего рукопожатия.

### Визуализация процесса

```
Клиент                                    Сервер
  │                                          │
  │  1. SYN (seq=100)                        │
  │ ──────────────────────────────────────► │
  │                                          │
  │  2. SYN-ACK (seq=300, ack=101)           │
  │ ◄────────────────────────────────────── │
  │                                          │
  │  3. ACK (seq=101, ack=301)               │
  │ ──────────────────────────────────────► │
  │                                          │
  │  ═══════════ Данные ═══════════         │
  │ ◄──────────────────────────────────────►│
```

### Шаг 1: SYN (Synchronize)

Клиент отправляет сегмент с флагом SYN и случайным начальным порядковым номером (ISN — Initial Sequence Number).

```
Флаги:  SYN=1, ACK=0
Seq:    100 (случайное число, выбранное клиентом)
Ack:    0 (нет подтверждения)

Смысл: «Привет, я хочу установить соединение.
        Мой начальный номер последовательности — 100»
```

**Зачем случайный ISN?** Для защиты от атак предсказания порядковых номеров (TCP sequence prediction attacks). Если ISN предсказуем, злоумышленник может внедрить данные в чужое соединение.

### Шаг 2: SYN-ACK (Synchronize-Acknowledge)

Сервер отвечает, подтверждая SYN клиента и отправляя собственный SYN со своим ISN.

```
Флаги:  SYN=1, ACK=1
Seq:    300 (случайное число, выбранное сервером)
Ack:    101 (ISN клиента + 1 = 100 + 1)

Смысл: «Получил твой запрос. Твой следующий байт должен
        иметь номер 101. Мой начальный номер — 300.
        Жду подтверждения.»
```

**Почему ACK = ISN+1?** Поле Acknowledgment Number означает «следующий ожидаемый байт». Получив байт с номером 100, сервер ожидает байт 101.

### Шаг 3: ACK (Acknowledge)

Клиент подтверждает получение SYN от сервера.

```
Флаги:  SYN=0, ACK=1
Seq:    101 (следующий байт от клиента)
Ack:    301 (ISN сервера + 1 = 300 + 1)

Смысл: «Принял твой ответ. Твой следующий байт
        должен иметь номер 301. Соединение установлено.»
```

После этого соединение установлено, и можно передавать данные.

### Завершение соединения (Four-Way Teardown)

```
Клиент                                    Сервер
  │  FIN (seq=500)                           │
  │ ──────────────────────────────────────► │
  │  ACK (ack=501)                           │
  │ ◄────────────────────────────────────── │
  │  FIN (seq=700)                           │
  │ ◄────────────────────────────────────── │
  │  ACK (ack=701)                           │
  │ ──────────────────────────────────────► │
```

### TCP RST (Reset) — экстренное закрытие

RST немедленно разрывает соединение без нормального завершения:
- Соединение к закрытому порту → сервер отвечает RST
- Нарушение протокола
- Принудительное завершение зависшего соединения
- **Атака RST injection** — злоумышленник подделывает RST для разрыва чужих соединений

### Сканирование портов и трёхстороннее рукопожатие

```
Порт ОТКРЫТ:
  Сканер: SYN  →  Сервер
  Сервер: SYN-ACK  →  Сканер
  Сканер: RST  →  Сервер (nmap не завершает соединение)

Порт ЗАКРЫТ:
  Сканер: SYN  →  Сервер
  Сервер: RST-ACK  →  Сканер

Порт ФИЛЬТРУЕТСЯ (firewall):
  Сканер: SYN  →  Сервер
  (нет ответа — timeout)
```

---

## 💻 Практика

### Задание 1: Исследование с помощью ping

```bash
# Базовый ping
ping google.com

# Указать количество пакетов
ping -c 4 google.com          # Linux/macOS
ping -n 4 google.com          # Windows

# Ping с указанием TTL
ping -t 64 google.com         # Linux (установить TTL)
ping -i 64 google.com         # macOS

# Ping с большим размером пакета (тест MTU)
ping -s 1400 google.com       # Linux
ping -l 1400 google.com       # Windows

# Определение ОС по TTL в ответе:
# TTL=64  → Linux/Unix/Android
# TTL=128 → Windows
# TTL=255 → Cisco IOS, некоторые Unix
```

Пример вывода и анализ:
```
PING google.com (142.250.185.238): 56 data bytes
64 bytes from 142.250.185.238: icmp_seq=0 ttl=117 time=12.345 ms
64 bytes from 142.250.185.238: icmp_seq=1 ttl=117 time=11.234 ms

Анализ:
- TTL=117: пакет прошёл 128-117=11 хопов (исходный TTL Windows = 128)
- time=12ms: задержка (latency) — приемлемо для международного узла
```

### Задание 2: Трассировка маршрута

```bash
# Linux/macOS
traceroute google.com

# Windows
tracert google.com

# Linux: использовать TCP вместо UDP (лучше проходит через firewall)
traceroute -T -p 80 google.com

# Linux: использовать ICMP
traceroute -I google.com

# Подробный вывод
traceroute -n google.com    # без разрешения имён (быстрее)
```

Пример вывода:
```
traceroute to google.com (142.250.185.238), 30 hops max
 1  192.168.1.1    1.234 ms   0.987 ms   1.012 ms   ← домашний роутер
 2  10.10.10.1     5.432 ms   5.123 ms   5.234 ms   ← роутер провайдера
 3  * * *                                            ← хоп не отвечает (ICMP заблокирован)
 4  72.14.215.85   11.23 ms  11.45 ms   11.12 ms   ← Google network
 5  142.250.185.238 12.34 ms 11.89 ms   12.01 ms   ← назначение

Звёздочки * * * означают:
- Маршрутизатор не отвечает на TTL-exceeded сообщения
- ICMP заблокирован файрволом
- Потеря пакетов (реже)
```

### Задание 3: Вычисление подсетей

```bash
# Установить ipcalc (Linux)
sudo apt-get install ipcalc

# Вычислить информацию о подсети
ipcalc 192.168.10.0/24

# Вывод:
# Network:   192.168.10.0/24
# Netmask:   255.255.255.0 = 24
# Broadcast: 192.168.10.255
# HostMin:   192.168.10.1
# HostMax:   192.168.10.254
# Hosts/Net: 254

# Разделить на подсети
ipcalc -s 50 30 10 192.168.1.0/24

# Установить sipcalc для расширенных вычислений
sudo apt-get install sipcalc
sipcalc 192.168.1.0/26
```

### Задание 4: Захват трёхстороннего рукопожатия

```bash
# Захватить TCP-трафик с хостом
sudo tcpdump -i eth0 host google.com and tcp -n

# В другом терминале инициировать соединение:
curl http://google.com

# Наблюдать в выводе tcpdump флаги SYN, SYN-ACK, ACK:
# Flags [S]  = SYN
# Flags [S.] = SYN-ACK (точка = ACK)
# Flags [.]  = ACK
# Flags [P.] = PSH-ACK (данные)
# Flags [F.] = FIN-ACK
```

---

## 🛠️ Задания

### ⭐ Задание 1 (Базовый уровень): Вычисление подсетей

Для следующих IP-адресов и масок определите:
- Сетевой адрес
- Broadcast-адрес
- Диапазон допустимых IP-адресов хостов
- Максимальное количество хостов

```
1. 192.168.5.100/27
2. 10.0.0.50/8
3. 172.20.15.200/20
4. 192.168.1.66/26
```

**Проверьте себя командой:** `ipcalc <IP>/<prefix>`

---

### ⭐⭐ Задание 2 (Средний уровень): Объяснение трёхстороннего рукопожатия

1. Запустите Wireshark или tcpdump
2. Откройте браузер и зайдите на `http://example.com` (не HTTPS!)
3. Найдите пакеты SYN, SYN-ACK, ACK
4. Запишите для каждого пакета:
   - Исходный IP и порт
   - Целевой IP и порт
   - Флаги TCP
   - Значения Sequence Number и Acknowledgment Number
5. Нарисуйте диаграмму последовательности (как в разделе 1.1.5)
6. Объясните, почему ACK-номер = предыдущий Seq + 1

---

### ⭐⭐⭐ Задание 3 (Продвинутый уровень): Нарисовать полную OSI-схему

Создайте детальную диаграмму, показывающую:

1. Все 7 уровней OSI с их номерами
2. Для каждого уровня: минимум 3 протокола/технологии
3. Тип адресации на каждом уровне (MAC/IP/Port/имя приложения)
4. Устройства сети, работающие на каждом уровне
5. Тип PDU (Protocol Data Unit) на каждом уровне:
   - L7-L5: Данные (Data)
   - L4: Сегмент (TCP) / Датаграмма (UDP)
   - L3: Пакет (Packet)
   - L2: Фрейм (Frame)
   - L1: Биты (Bits)
6. Покажите процесс инкапсуляции HTTP-запроса через все уровни
7. Укажите по одной типичной атаке на каждый уровень

Используйте draw.io, Excalidraw или нарисуйте от руки.

---

## ✅ Чеклист готовности

- [ ] Я могу назвать все 7 уровней OSI в правильном порядке без подсказки
- [ ] Я понимаю разницу между моделью OSI и стеком TCP/IP
- [ ] Я умею вычислять сетевой адрес, broadcast и диапазон хостов по IP/маске
- [ ] Я понимаю нотацию CIDR и могу перевести /24 в 255.255.255.0 и обратно
- [ ] Я могу объяснить разницу TCP и UDP и привести примеры использования каждого
- [ ] Я могу пошагово объяснить трёхстороннее рукопожатие TCP с указанием флагов и номеров
- [ ] Я умею использовать ping для проверки связи и traceroute для трассировки маршрута
- [ ] Я понимаю, что означают звёздочки в выводе traceroute
- [ ] Я могу определить ОС по значению TTL в ping-ответе
- [ ] Я умею захватить трёхстороннее рукопожатие с помощью tcpdump

---

## 🔗 Ресурсы

### Официальные документы (RFC)
- [RFC 791 — Internet Protocol (IPv4)](https://www.rfc-editor.org/rfc/rfc791)
- [RFC 793 — Transmission Control Protocol](https://www.rfc-editor.org/rfc/rfc793)
- [RFC 768 — User Datagram Protocol](https://www.rfc-editor.org/rfc/rfc768)
- [RFC 1918 — Private Internet Addresses](https://www.rfc-editor.org/rfc/rfc1918)
- [RFC 4632 — CIDR](https://www.rfc-editor.org/rfc/rfc4632)

### Онлайн-инструменты
- [Subnet Calculator — subnet-calculator.com](https://www.subnet-calculator.com)
- [CIDR.xyz — визуальный калькулятор подсетей](https://cidr.xyz)
- [Packet Sender — тест TCP/UDP соединений](https://packetsender.com)

### Учебные материалы
- [Cisco Networking Academy — Introduction to Networks](https://www.netacad.com)
- [Professor Messer — CompTIA Network+](https://www.professormesser.com/network-plus/n10-008/n10-008-video/n10-008-training-course/)
- [Practical Networking — Subnetting Mastery](https://www.practicalnetworking.net/stand-alone/subnetting-mastery/)

### Книги
- Andrew Tanenbaum — «Computer Networks» (5th Edition)
- W. Richard Stevens — «TCP/IP Illustrated, Volume 1»
- James Kurose, Keith Ross — «Computer Networking: A Top-Down Approach»
