# Глава 1.4: Сетевые инструменты и основы Wireshark

## 🎯 Цели главы

- Освоить диагностические инструменты: ping, traceroute, netstat/ss, nslookup/dig, curl
- Понять интерфейс Wireshark и создавать эффективные фильтры отображения
- Научиться следовать TCP-потокам и восстанавливать данные из захватов
- Применять Wireshark для обнаружения атак: сканирование, ARP spoofing, DNS exfiltration
- Строить фильтры, применяемые в SOC-операциях

---

## 1.4.1 ping: ICMP и диагностика связи

### Как работает ping

`ping` использует протокол ICMP (Internet Control Message Protocol). Клиент отправляет ICMP Echo Request, получатель отвечает ICMP Echo Reply.

```
Отправитель                              Получатель
  │  ICMP Echo Request (type=8, code=0)    │
  │  seq=1, TTL=64                          │
  │ ──────────────────────────────────────►│
  │                                         │
  │  ICMP Echo Reply (type=0, code=0)       │
  │  seq=1, TTL=117                         │
  │ ◄────────────────────────────────────── │
```

### TTL и определение ОС

TTL (Time To Live) уменьшается на 1 при прохождении каждого маршрутизатора. Начальное значение TTL зависит от ОС.

```
Стандартные начальные TTL:
  Windows:         128
  Linux/Android:   64
  macOS:           64
  Cisco IOS:       255
  FreeBSD:         64
  Solaris:         255

Определение ОС по TTL в ответе:
  TTL 117 → 128 - 117 = 11 хопов → Windows
  TTL 53  → 64  - 53  = 11 хопов → Linux
  TTL 244 → 255 - 244 = 11 хопов → Cisco
```

### Команды ping

```bash
# Базовое использование
ping 8.8.8.8
ping google.com

# Linux/macOS флаги:
ping -c 5 google.com           # отправить 5 пакетов (count)
ping -i 2 google.com           # интервал 2 секунды (interval)
ping -s 1400 google.com        # размер пакета 1400 байт (size)
ping -W 1 google.com           # таймаут 1 секунда (wait)
ping -q google.com             # тихий режим — только статистика (quiet)
ping -f google.com             # flood ping (требует root!)
ping -t 10 google.com          # установить TTL=10 (пакет дойдёт до 10-го хопа)

# Windows флаги:
ping -n 5 google.com           # количество пакетов
ping -l 1400 google.com        # размер пакета
ping -w 1000 google.com        # таймаут в мс
ping -i 10 google.com          # TTL
ping -t google.com             # бесконечный ping (Ctrl+C для остановки)

# Пример вывода и интерпретация:
PING google.com (142.250.185.238): 56 data bytes
64 bytes from 142.250.185.238: icmp_seq=0 ttl=117 time=12.3 ms
64 bytes from 142.250.185.238: icmp_seq=1 ttl=117 time=11.8 ms
64 bytes from 142.250.185.238: icmp_seq=2 ttl=117 time=12.1 ms
64 bytes from 142.250.185.238: icmp_seq=3 ttl=117 time=11.9 ms

--- google.com ping statistics ---
4 packets transmitted, 4 received, 0% packet loss, time 3004ms
rtt min/avg/max/mdev = 11.8/12.0/12.3/0.2 ms

Интерпретация:
  - 0% packet loss → связь стабильная
  - avg RTT 12ms → хорошая задержка для международного хоста
  - mdev (mean deviation) 0.2ms → очень стабильная задержка (джиттер мал)
  - TTL 117: начальный 128 → Windows-сервер, 11 хопов до цели
```

### ICMP типы и их значение

```
Type  Code  Описание
────────────────────────────────────────────
0     0     Echo Reply (ответ на ping)
3     0     Destination Net Unreachable (сеть недостижима)
3     1     Destination Host Unreachable (хост недостижим)
3     3     Destination Port Unreachable (порт недостижим — UDP)
3     4     Fragmentation Needed (нужна фрагментация — для Path MTU Discovery)
3     13    Communication Administratively Prohibited (файрвол заблокировал)
5     0     Redirect for Network (перенаправление маршрута)
8     0     Echo Request (запрос ping)
11    0     TTL Exceeded in Transit (TTL истёк — traceroute использует это!)
```

```bash
# Захватить ICMP в tcpdump
sudo tcpdump -i eth0 icmp -v

# В Wireshark:
# Фильтр: icmp
# Видны все типы ICMP
```

---

## 1.4.2 traceroute / tracert: трассировка маршрута

### Принцип работы

traceroute использует TTL механизм ICMP. При TTL=0 маршрутизатор отбрасывает пакет и отправляет ICMP Time Exceeded обратно — раскрывая свой IP.

```
Шаг 1: Отправляем пакет с TTL=1
  → Первый маршрутизатор уменьшает TTL до 0
  → Отправляет ICMP "Time Exceeded" обратно
  → Мы узнаём IP первого хопа

Шаг 2: Отправляем пакет с TTL=2
  → Первый хоп пропускает (TTL стало 1)
  → Второй хоп уменьшает до 0, отправляет "Time Exceeded"
  → Мы узнаём IP второго хопа

...и так до достижения цели
```

```
Клиент          Хоп1            Хоп2            Цель
  │  TTL=1        │               │               │
  │ ─────────────►│               │               │
  │  ICMP Time    │               │               │
  │  Exceeded     │               │               │
  │ ◄─────────────│               │               │
  │  TTL=2        │               │               │
  │ ──────────────┼──────────────►│               │
  │               │  ICMP Time    │               │
  │               │  Exceeded     │               │
  │ ◄─────────────┼───────────────│               │
  │  TTL=3        │               │               │
  │ ──────────────┼───────────────┼──────────────►│
  │               │               │  ICMP Echo    │
  │               │               │  Reply        │
  │ ◄─────────────┼───────────────┼───────────────│
```

### Различия реализаций

```
Linux traceroute:
  - По умолчанию использует UDP (порты 33434+)
  - Каждый хоп зондируется 3 пакетами
  - Цель отвечает ICMP Port Unreachable (UDP на закрытый порт)

Windows tracert:
  - Использует ICMP Echo Request (как ping)
  - Лучше проходит через файрволы
  - Медленнее (1 секунда таймаут по умолчанию)

macOS traceroute:
  - Аналог Linux
```

### Команды traceroute

```bash
# Linux
traceroute google.com

# Флаги:
traceroute -n google.com        # без DNS-разрешения (быстрее)
traceroute -m 30 google.com     # максимальное количество хопов (max TTL)
traceroute -q 1 google.com     # 1 зонд на хоп (по умолчанию 3)
traceroute -w 2 google.com     # таймаут 2 секунды
traceroute -T google.com       # использовать TCP вместо UDP (лучше через файрволы)
traceroute -T -p 80 google.com # TCP на порт 80
traceroute -I google.com       # использовать ICMP (как Windows tracert)

# Windows
tracert google.com
tracert -d google.com          # без разрешения имён
tracert -h 15 google.com       # максимум 15 хопов
tracert -w 500 google.com      # таймаут 500мс

# macOS (аналог Linux)
traceroute -n google.com

# Альтернативы:
mtr google.com                  # комбинация ping+traceroute в реальном времени
mtr --report google.com         # статистика за 10 зондов
```

### Интерпретация результатов

```bash
$ traceroute -n google.com
traceroute to google.com (142.250.185.238), 30 hops max

 1  192.168.1.1      1.2 ms   0.9 ms   1.1 ms   ← домашний роутер
 2  10.10.5.1        5.4 ms   5.2 ms   5.3 ms   ← роутер провайдера (CGNAT)
 3  * * *                                        ← ICMP заблокирован (нормально)
 4  172.16.100.1    15.3 ms  14.9 ms  15.1 ms   ← backbone провайдера
 5  72.14.215.85    12.2 ms  11.8 ms  12.0 ms   ← Google edge router
 6  142.250.185.238 12.4 ms  12.1 ms  12.3 ms   ← цель

Интерпретация:
  Хоп 1: задержка ~1мс — локальная сеть
  Хоп 2: задержка ~5мс — соединение с провайдером
  Хоп 3: * * * — маршрутизатор не отвечает на TTL Exceeded
           (ICMP заблокирован, но трафик идёт дальше)
  Хоп 4-5: рост задержки — трафик идёт через WAN
  Хоп 6: финальная задержка 12мс
```

**Что означают звёздочки `* * *`:**
1. Маршрутизатор настроен не отвечать на ICMP TTL Exceeded (Privacy/Security)
2. ICMP заблокирован файрволом на данном хопе
3. Пакет потерян (редко)
4. Асимметричная маршрутизация (ответ идёт другим путём)

> Звёздочки НЕ означают разрыв трассировки — следующий хоп может отвечать нормально!

---

## 1.4.3 netstat / ss: активные соединения

### netstat (традиционный)

```bash
# Все соединения
netstat -a

# TCP/UDP с портами (без DNS-разрешения)
netstat -tuln

# С именами процессов (требует root)
sudo netstat -tulnp

# Только установленные соединения
netstat -tn

# Только слушающие порты
netstat -l

# Статистика протоколов
netstat -s

# Таблица маршрутизации
netstat -r

# Флаги:
# -t  TCP
# -u  UDP
# -l  LISTEN (слушающие)
# -n  числовые IP/порты
# -p  показать PID/процесс
# -a  все состояния
```

### ss (современная замена netstat)

```bash
# Аналог netstat -tulnp
ss -tulnp

# Все TCP-соединения
ss -t

# Только слушающие
ss -l

# Фильтр по порту
ss -t '( dport = :22 or sport = :22 )'

# Фильтр по состоянию
ss -t state established

# Расширенная информация
ss -e -i

# Пример вывода ss -tulnp:
Netid State  Recv-Q Send-Q  Local Address:Port  Peer Address:Port  Process
tcp   LISTEN 0      128     0.0.0.0:22           0.0.0.0:*         users:(("sshd",pid=1234,fd=3))
tcp   LISTEN 0      511     0.0.0.0:80           0.0.0.0:*         users:(("nginx",pid=5678,fd=6))
tcp   LISTEN 0      128     127.0.0.1:3306       0.0.0.0:*         users:(("mysqld",pid=9012,fd=21))
tcp   ESTAB  0      0       192.168.1.10:22      192.168.1.5:54321 users:(("sshd",pid=1456,fd=4))
```

### Состояния TCP-соединений

```
LISTEN      — Сервер ожидает входящих соединений на порту
SYN_SENT    — Клиент отправил SYN, ожидает SYN-ACK
SYN_RECV    — Сервер получил SYN, отправил SYN-ACK, ожидает ACK
ESTABLISHED — Соединение установлено, передаются данные
FIN_WAIT_1  — Клиент отправил FIN, ожидает ACK
FIN_WAIT_2  — Клиент получил ACK на FIN, ожидает FIN от сервера
CLOSE_WAIT  — Получен FIN от удалённой стороны, ожидание закрытия приложения
CLOSING     — Оба конца отправили FIN одновременно
LAST_ACK    — Сервер отправил FIN, ожидает последнего ACK
TIME_WAIT   — Ожидание 2*MSL для перехвата запоздалых пакетов (2 минуты)
CLOSED      — Соединение закрыто
```

**Важные состояния для безопасности:**

`TIME_WAIT` в большом количестве — нормально при высокой нагрузке. Если тысячи `TIME_WAIT` — возможна атака или неправильная конфигурация.

`SYN_RECV` в большом количестве — признак SYN flood атаки:
```bash
# Обнаружение SYN flood
ss -t state syn-recv | wc -l
# Если > 100 — возможна атака
```

---

## 1.4.4 nslookup и dig: DNS из командной строки

### nslookup

```bash
# Простой запрос
nslookup google.com

# Запрос к конкретному DNS-серверу
nslookup google.com 8.8.8.8
nslookup google.com 1.1.1.1

# Запрос конкретного типа записи
nslookup -type=MX gmail.com
nslookup -type=TXT google.com
nslookup -type=NS google.com
nslookup -type=AAAA google.com

# Обратный DNS
nslookup 8.8.8.8

# Интерактивный режим
nslookup
> server 8.8.8.8            # изменить DNS-сервер
> set type=MX               # изменить тип запроса
> gmail.com                 # запрос
> set debug                 # режим отладки
> exit
```

### dig (продвинутый DNS-клиент)

```bash
# Базовый запрос
dig google.com

# Только ответ (краткий)
dig +short google.com

# Конкретный тип записи
dig A google.com
dig AAAA google.com
dig MX gmail.com
dig TXT google.com
dig NS google.com
dig SOA google.com
dig CNAME www.github.com
dig PTR 8.8.8.8.in-addr.arpa    # обратный DNS
dig -x 8.8.8.8                   # то же, автоматически

# Использовать конкретный DNS-сервер
dig @8.8.8.8 google.com
dig @1.1.1.1 google.com
dig @ns1.google.com google.com   # запрос к авторитативному NS

# Трассировка DNS-запроса (от root до authoritative)
dig +trace google.com

# Без рекурсии (только локальный кеш сервера)
dig +norecurse google.com

# Запросить все записи
dig ANY google.com

# Отключить дополнительные секции
dig +noall +answer google.com

# Измерить время DNS-запроса
dig +stats google.com

# Подписанные DNS-записи (DNSSEC)
dig +dnssec google.com
dig DS google.com

# Пример полного вывода dig:
; <<>> DiG 9.18.0 <<>> google.com
;; global options: +cmd
;; Got answer:
;; ->>HEADER<<- opcode: QUERY, status: NOERROR, id: 12345
;; flags: qr rd ra; QUERY: 1, ANSWER: 6, AUTHORITY: 0, ADDITIONAL: 1

;; QUESTION SECTION:
;google.com.                    IN      A

;; ANSWER SECTION:
google.com.             300     IN      A       142.250.185.238
google.com.             300     IN      A       142.250.185.174
...

;; Query time: 12 msec
;; SERVER: 192.168.1.1#53(192.168.1.1)
;; WHEN: Mon Jan 01 12:00:00 UTC 2024
;; MSG SIZE  rcvd: 135

Интерпретация:
  status: NOERROR → запрос успешен
  flags: qr (query response) rd (recursion desired) ra (recursion available)
  TTL: 300 секунд
  SERVER: наш DNS-resolver 192.168.1.1
```

---

## 1.4.5 curl: HTTP из командной строки

### Базовые запросы

```bash
# GET-запрос
curl https://httpbin.org/get

# Подробный вывод (заголовки + тело)
curl -v https://httpbin.org/get

# Только заголовки ответа
curl -I https://google.com         # HEAD-запрос
curl -D - https://google.com       # тело + заголовки (- = stdout)

# Следовать редиректам
curl -L https://google.com

# Скачать файл
curl -O https://example.com/file.txt           # имя из URL
curl -o myfile.txt https://example.com/file.txt # своё имя
```

### Заголовки и методы

```bash
# POST с JSON
curl -X POST \
     -H "Content-Type: application/json" \
     -d '{"key": "value"}' \
     https://httpbin.org/post

# POST с form data
curl -X POST \
     -d "username=admin&password=secret" \
     https://httpbin.org/post

# PUT-запрос
curl -X PUT \
     -H "Content-Type: application/json" \
     -d '{"name": "Alice"}' \
     https://api.example.com/users/1

# DELETE-запрос
curl -X DELETE https://api.example.com/users/1

# Кастомные заголовки
curl -H "Authorization: Bearer eyJ..." \
     -H "X-Custom: value" \
     -H "User-Agent: CustomBot/1.0" \
     https://api.example.com/protected

# Cookie
curl -b "session=abc123; token=xyz" https://example.com
curl -c cookies.txt -b cookies.txt https://example.com  # сохранять/загружать
```

### Полезные флаги для SOC и пентеста

```bash
# Игнорировать TLS-ошибки (для анализа)
curl -k https://self-signed.example.com

# Указать сертификат клиента (mTLS)
curl --cert client.crt --key client.key https://api.example.com

# Через прокси (Burp Suite, OWASP ZAP)
curl -x http://127.0.0.1:8080 https://example.com
curl --proxy-insecure -x http://127.0.0.1:8080 https://example.com

# Измерить производительность
curl -w "
DNS: %{time_namelookup}s
Connect: %{time_connect}s
TLS: %{time_appconnect}s
TTFB: %{time_starttransfer}s
Total: %{time_total}s
Size: %{size_download} bytes
" -o /dev/null -s https://google.com

# Ограничить скорость (имитация медленного соединения)
curl --limit-rate 100k https://example.com/file.zip

# Продолжить прерванную загрузку
curl -C - -O https://example.com/large-file.iso

# Отправить multipart/form-data (загрузка файла)
curl -F "file=@/path/to/file.txt" \
     -F "description=my file" \
     https://example.com/upload

# URL encode данные
curl --data-urlencode "search=hello world" https://example.com

# Несколько запросов
curl -s https://api.example.com/users \
     https://api.example.com/posts

# Параллельные запросы (curl 7.66+)
curl --parallel --parallel-max 10 \
     -O "https://example.com/file[1-10].txt"
```

---

## 1.4.6 Wireshark: интерфейс и основы

### Интерфейс Wireshark

```
┌─────────────────────────────────────────────────────────────────┐
│  File  Edit  View  Go  Capture  Analyze  Statistics  ...        │
├─────────────────────────────────────────────────────────────────┤
│  [Фильтр отображения] ip.addr == 192.168.1.1         [Apply]   │
├─────────────────────────────────────────────────────────────────┤
│  No.  Time     Source          Dest            Protocol  Info   │
│   1   0.000   192.168.1.10   192.168.1.1     ARP       Who..  │
│   2   0.001   192.168.1.1    192.168.1.10    ARP       Reply  │
│   3   0.002   192.168.1.10   8.8.8.8         DNS       Std Q  │
│   4   0.012   8.8.8.8        192.168.1.10    DNS       Std R  │
│   5   0.013   192.168.1.10   142.250.185.238 TCP       SYN    │
│   6   0.025   142.250.185.238 192.168.1.10   TCP       SYN-ACK│
│   7   0.025   192.168.1.10   142.250.185.238 TCP       ACK    │
│   8   0.026   192.168.1.10   142.250.185.238 HTTP      GET /  │
├─────────────────────────────────────────────────────────────────┤
│  ▼ Frame 8: 512 bytes on wire, 512 bytes captured               │
│  ▼ Ethernet II, Src: ..., Dst: ...                              │
│  ▼ Internet Protocol Version 4, Src: ..., Dst: ...             │
│  ▼ Transmission Control Protocol, Src Port: 51234, Dst: 80     │
│  ▼ Hypertext Transfer Protocol                                  │
│    GET / HTTP/1.1\r\n                                           │
│    Host: example.com\r\n                                        │
├─────────────────────────────────────────────────────────────────┤
│  HEX/ASCII DUMP:                                                │
│  0000  45 00 02 00 00 01 40 00  40 06 00 00 c0 a8 01 0a  E...  │
└─────────────────────────────────────────────────────────────────┘
```

### Фильтры захвата (Capture Filters — BPF синтаксис)

Применяются ДО захвата, уменьшают объём данных:

```bash
# Только трафик к/от IP
host 192.168.1.1
src host 192.168.1.1
dst host 192.168.1.1

# По порту
port 80
src port 443
dst port 53

# По протоколу
tcp
udp
icmp
arp

# Комбинирование
host 192.168.1.1 and port 80
tcp and dst port 443
not arp and not dns

# Подсеть
net 192.168.1.0/24

# Диапазон портов
portrange 1-1024
```

### Фильтры отображения (Display Filters — Wireshark синтаксис)

Применяются ПОСЛЕ захвата, не меняют данные:

```bash
# IP-адреса
ip.addr == 192.168.1.1
ip.src == 192.168.1.10
ip.dst == 8.8.8.8
ip.addr == 192.168.1.0/24      # подсеть

# Порты
tcp.port == 80
tcp.srcport == 443
tcp.dstport == 22
udp.port == 53

# Протоколы
http
https
dns
arp
icmp
ftp
ssh
smb
rdp
telnet

# TCP флаги
tcp.flags.syn == 1              # SYN-пакеты
tcp.flags.syn == 1 and tcp.flags.ack == 0  # только SYN (не SYN-ACK)
tcp.flags.reset == 1            # RST-пакеты
tcp.flags.fin == 1              # FIN-пакеты

# HTTP
http.request.method == "GET"
http.request.method == "POST"
http.response.code == 200
http.response.code == 404
http.response.code >= 400       # все ошибки
http.host == "example.com"
http.request.uri contains "login"
http.cookie contains "session"

# DNS
dns.qry.name == "google.com"
dns.qry.name contains "evil"
dns.resp.addr == "8.8.8.8"
dns.flags.response == 0         # только запросы

# Размер пакета
frame.len > 1000
frame.len < 100

# Временной диапазон
frame.time_relative >= 10 and frame.time_relative <= 20

# Строка в данных
frame contains "password"
tcp contains "SELECT"
http contains "admin"

# Комбинирование (AND, OR, NOT)
ip.addr == 192.168.1.1 and http
dns or arp
not ip.addr == 192.168.1.1
(tcp.port == 80 or tcp.port == 443) and ip.dst != 192.168.1.1
```

---

## 1.4.7 Продвинутые возможности Wireshark

### Follow TCP Stream

Позволяет просмотреть всю TCP-сессию в читаемом виде:

```
Правый клик на пакете → Follow → TCP Stream

Показывает:
  Красный текст: данные от клиента к серверу
  Синий текст:   данные от сервера к клиенту

Применение:
  - Восстановление HTTP-запросов и ответов
  - Просмотр FTP-команд и файлов
  - Анализ Telnet-сессий (видны пароли!)
  - Декодирование любого текстового протокола
```

### Экспорт объектов из HTTP

```
Analyze → Follow HTTP Stream → для просмотра

File → Export Objects → HTTP
→ Список всех файлов, переданных по HTTP:
   - HTML-страницы
   - Изображения (JPEG, PNG)
   - JavaScript-файлы
   - Загруженные документы

Это позволяет "скачать" файлы из перехваченного трафика!
```

### Статистика

```
Statistics → Protocol Hierarchy
→ Показывает распределение протоколов (% байт, % пакетов)

Statistics → Conversations
→ Список всех соединений (топ по объёму трафика)

Statistics → Endpoints
→ Список всех IP-адресов с объёмом трафика

Statistics → IO Graph
→ График трафика во времени

Statistics → Flow Graph
→ Диаграмма последовательности пакетов
```

### Цветовые правила (Coloring Rules)

По умолчанию Wireshark выделяет цветом:
```
Зелёный: TCP нормальный трафик
Тёмно-зелёный: HTTP
Синий: DNS
Голубой: TCP SYN/FIN
Жёлтый: ARP
Красный: ошибки TCP (retransmissions, RST)
Чёрный: ошибки (malformed packets)
```

---

## 1.4.8 Фильтры Wireshark для SOC

### Обнаружение SYN flood (DDoS-атака)

```
# Большое количество SYN без ACK от одного источника:
tcp.flags.syn == 1 and tcp.flags.ack == 0

# Признаки:
# - Много пакетов с одного IP на разные порты/хосты
# - Нет завершения рукопожатия (нет SYN-ACK ответа или он игнорируется)

# Дополнительный фильтр — статистика:
Statistics → Conversations → TCP → Sort by packets
# Видим: один IP создаёт тысячи "соединений"
```

### Обнаружение сканирования портов nmap

```
# SYN-сканирование (Stealth Scan):
# nmap -sS → посылает SYN, при получении SYN-ACK сразу отправляет RST
tcp.flags.syn == 1 and tcp.flags.reset == 1

# Признаки:
# - Один src IP делает SYN к множеству dst портов
# - После SYN-ACK сразу RST (не завершает handshake)
# - Много RST-ACK ответов от сервера (закрытые порты)

# NULL/FIN/XMAS сканирование (необычные флаги):
tcp.flags == 0x000          # NULL scan
tcp.flags == 0x001          # FIN scan
tcp.flags == 0x029          # XMAS scan (FIN+PSH+URG)
```

### Обнаружение ARP Spoofing

```
# Дублирующиеся ARP-ответы:
arp.opcode == 2                              # ARP Reply
# Смотрим: один MAC заявляет о двух разных IP
# Один IP заявляется двумя разными MAC

# В Wireshark автоматически:
# Expert Information → Warnings → ARP duplicate address detected

# Wireshark встроенный детектор:
# Edit → Preferences → Protocols → ARP/RARP → Detect ARP request storms
```

### Обнаружение DNS exfiltration (утечки данных через DNS)

```
# Длинные DNS-запросы (данные кодируются в поддоменах):
dns && frame.len > 150
dns.qry.name matches ".{40,}"               # длинные имена

# Высокая частота DNS к одному домену:
dns.qry.name contains ".evil.com"
# Statistics → DNS → Sort by Count

# Необычные типы записей (NULL, ANY для туннелирования):
dns.qry.type == 255                         # ANY запросы
dns.qry.type == 10                          # NULL record

# Высокая энтропия (base64 в субдомене):
# Визуально: много случайных символов в субдомене
# Инструмент: CapLoader или специализированные IDS
```

### Обнаружение учётных данных в открытом виде

```
# HTTP Basic Auth (base64)
http.authorization

# FTP логины
ftp.request.command == "USER"
ftp.request.command == "PASS"

# Telnet сессии (всё в открытом виде)
telnet

# SMTP AUTH
smtp.req.command == "AUTH"

# HTTP формы (логины, пароли)
http.request.method == "POST" and http contains "password"
```

### Комплексные SOC-фильтры

```
# Исключить шумный трафик (broadcast, ARP, DNS) для анализа подозрительного
not (arp or dns or icmp or broadcast)

# Показать только внешний трафик (за пределы подсети)
not ip.dst == 192.168.1.0/24 and not ip.dst == 10.0.0.0/8

# Медленное соединение (потенциальные slow loris атаки)
http && tcp.time_delta > 5

# Очень большие HTTP-ответы (возможная утечка данных)
http.response && frame.len > 100000

# Внутренняя разведка (сканирование через ICMP)
icmp.type == 8 and ip.src == 192.168.1.0/24
```

---

## 💻 Практика

### Задание 1: Захват DNS-запроса

```bash
# 1. Очистить DNS-кеш
sudo systemd-resolve --flush-caches    # Linux (systemd)
sudo dscacheutil -flushcache           # macOS
ipconfig /flushdns                     # Windows

# 2. Запустить захват
sudo tcpdump -i eth0 port 53 -w dns_capture.pcap

# 3. В другом терминале выполнить DNS-запрос
dig google.com
curl -s https://httpbin.org/get

# 4. Остановить захват (Ctrl+C)

# 5. Открыть в Wireshark
wireshark dns_capture.pcap &

# 6. Применить фильтр: dns
# 7. Проследить запрос и ответ
# 8. Нажать на A-запись → посмотреть все поля в packet details
```

### Задание 2: Захват полной HTTP-сессии

```bash
# 1. Запустить захват
sudo tcpdump -i eth0 host httpbin.org -w http_session.pcap

# 2. Выполнить HTTP-запрос
curl -v http://httpbin.org/get
curl -X POST -d '{"test": "data"}' -H "Content-Type: application/json" http://httpbin.org/post

# 3. Остановить захват

# 4. Открыть в Wireshark
# 5. Применить фильтр: http
# 6. Найти GET-запрос → правый клик → Follow → HTTP Stream
# 7. Экспортировать объекты: File → Export Objects → HTTP
```

### Задание 3: Применить фильтры для анализа

```bash
# Скачать тестовый pcap-файл
wget https://wiki.wireshark.org/uploads/27707187aeb30df68e70c8fb9d614981/http.cap
# Или использовать встроенные примеры: File → Open Sample Capture

# Применить по очереди фильтры и описать, что видите:
# 1. http
# 2. http.request.method == "GET"
# 3. tcp.flags.syn == 1
# 4. dns
# 5. ip.addr == 65.208.228.223
# 6. tcp.stream eq 0      ← первый TCP-поток
```

---

## 🛠️ Задания

### ⭐ Задание 1 (Базовый уровень): Диагностика сети инструментами

Проведите диагностику соединения с сайтом `cloudflare.com`:

1. `ping -c 10 cloudflare.com` — записать среднюю задержку и % потерь
2. `traceroute -n cloudflare.com` — записать количество хопов, объяснить звёздочки
3. `dig cloudflare.com` — записать IP, TTL, время запроса, DNS-сервер
4. `curl -v https://cloudflare.com 2>&1 | head -50` — записать HTTP-код ответа
5. `ss -tulnp | grep -E "LISTEN"` — записать все слушающие сервисы на вашей машине

Оформите отчёт: опишите маршрут пакета от вашей машины до cloudflare.com.

---

### ⭐⭐ Задание 2 (Средний уровень): Анализ захвата в Wireshark

1. Скачайте тестовый pcap: https://www.malware-traffic-analysis.net/ (публичные samples)
   Или создайте свой: `sudo tcpdump -i eth0 -w capture.pcap` (5 минут обычной активности)

2. В Wireshark ответьте на вопросы:
   - Сколько уникальных IP-адресов в захвате? (Statistics → Endpoints)
   - Какой протокол генерирует больше всего трафика? (Statistics → Protocol Hierarchy)
   - Найдите TCP-соединение с максимальным объёмом данных (Statistics → Conversations)
   - Есть ли в захвате незашифрованные учётные данные (HTTP Basic Auth, FTP, Telnet)?
   - Есть ли подозрительные DNS-запросы (очень длинные имена)?

3. Создайте список из 10 наблюдений с применёнными фильтрами

---

### ⭐⭐⭐ Задание 3 (Продвинутый уровень): Обнаружение атак в pcap

Скачайте учебный pcap с атаками:
- https://www.malware-traffic-analysis.net/2023/ (реальные образцы вредоносного ПО)
- https://wiki.wireshark.org/SampleCaptures

Для каждого файла:

1. **Определите тип атаки** по следующим признакам:
   - Сканирование портов: много SYN → разные dst ports от одного src
   - ARP spoofing: дублирующиеся ARP Reply с разными MAC для одного IP
   - DNS exfiltration: длинные субдомены, высокая частота запросов к одному домену
   - DDoS: аномально высокий объём трафика от множества источников

2. **Восстановите временную шкалу атаки** — когда началась, сколько длилась, что произошло

3. **Найдите индикаторы компрометации (IoC)**:
   - IP-адреса атакующих
   - Домены, к которым обращалось вредоносное ПО
   - Имена файлов (из HTTP Export Objects)
   - Пользовательские агенты

4. Напишите отчёт SOC-аналитика с рекомендациями по реагированию

---

## ✅ Чеклист готовности

- [ ] Я понимаю, как ping использует ICMP и умею читать вывод ping
- [ ] Я могу определить ОС по TTL в ping-ответе
- [ ] Я понимаю принцип traceroute и могу объяснить каждый хоп
- [ ] Я знаю, что означают звёздочки в traceroute и как они появляются
- [ ] Я умею использовать ss/netstat для просмотра открытых соединений и их состояний
- [ ] Я понимаю состояния TCP и могу объяснить TIME_WAIT и SYN_RECV
- [ ] Я умею использовать dig для трассировки DNS и nslookup для быстрых запросов
- [ ] Я могу сформировать curl-запрос с кастомными заголовками, методом и телом
- [ ] Я умею писать фильтры отображения Wireshark для IP, портов, протоколов, флагов
- [ ] Я умею использовать Follow TCP Stream для восстановления HTTP-сессий
- [ ] Я могу применить фильтры для обнаружения SYN flood, ARP spoofing, DNS exfiltration
- [ ] Я умею экспортировать объекты из HTTP-трафика в Wireshark

---

## 🔗 Ресурсы

### Официальные документы
- [Wireshark User's Guide](https://www.wireshark.org/docs/wsug_html_chunked/)
- [Wireshark Display Filter Reference](https://www.wireshark.org/docs/dfref/)
- [Wireshark Capture Filter (BPF) Reference](https://wiki.wireshark.org/CaptureFilters)
- [curl Man Page](https://curl.se/docs/manpage.html)

### Практика и образцы pcap
- [Wireshark Sample Captures](https://wiki.wireshark.org/SampleCaptures)
- [Malware Traffic Analysis (публичные pcap)](https://www.malware-traffic-analysis.net)
- [PacketTotal — онлайн-анализ pcap](https://packettotal.com)

### Онлайн-инструменты
- [CloudShark — Wireshark в браузере](https://www.cloudshark.org)
- [A-Packets — анализ pcap онлайн](https://apackets.com)

### Дополнительные инструменты
- [tshark — командная строка Wireshark](https://www.wireshark.org/docs/man-pages/tshark.html)
- [tcpdump manual](https://www.tcpdump.org/manpages/tcpdump.1.html)
- [mtr — My Traceroute](https://github.com/traviscross/mtr)

### Книги
- Chris Sanders — «Practical Packet Analysis» (3rd Edition)
- Laura Chappell — «Wireshark Network Analysis»
