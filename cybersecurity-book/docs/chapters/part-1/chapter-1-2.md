# Глава 1.2: DNS и HTTP/HTTPS в деталях

## 🎯 Цели главы

- Понять полный цикл DNS-запроса от браузера до авторитативного сервера
- Изучить все основные типы DNS-записей с практическими примерами
- Понять, как DNS используется как вектор атаки
- Освоить HTTP-методы, коды ответов и заголовки
- Понять механизм TLS-рукопожатия и работу HTTPS
- Научиться использовать dig, nslookup, curl для практического анализа

---

## 1.2.1 Как работает DNS

DNS (Domain Name System) — это распределённая иерархическая база данных, которая преобразует доменные имена (например, `google.com`) в IP-адреса (например, `142.250.185.238`).

### Зачем нужен DNS?

Людям трудно запоминать IP-адреса. DNS позволяет использовать понятные имена:
- Вместо `142.250.185.238` — `google.com`
- Вместо `151.101.1.140` — `reddit.com`

DNS также обеспечивает гибкость: если компания переезжает на новый сервер, достаточно обновить DNS-запись, и пользователям не нужно знать новый IP.

### Иерархия DNS

```
                    . (корень / root)
                   /|\
                  / | \
                .com .org .net .ru ...  ← TLD (Top-Level Domain)
               /
           google.com.                  ← Authoritative zone
          /          \
    www.google.com   mail.google.com   ← Поддомены / записи
```

### Компоненты DNS-системы

**DNS Resolver (Recursive Resolver)**
- Обычно предоставляется провайдером или используется публичный (8.8.8.8, 1.1.1.1)
- Получает запрос от клиента и выполняет всю «работу» по поиску ответа
- Кеширует результаты для ускорения повторных запросов

**Root Nameservers**
- 13 групп корневых серверов (a.root-servers.net до m.root-servers.net)
- Знают адреса серверов TLD
- Физически их тысячи, благодаря anycast-маршрутизации

**TLD Nameservers (Top-Level Domain)**
- Управляются ICANN и организациями (Verisign для .com)
- Знают адреса авторитативных серверов для доменов в своей зоне

**Authoritative Nameservers**
- Хранят фактические DNS-записи для домена
- Дают окончательный ответ на запрос

---

### Полный путь DNS-запроса (шаг за шагом)

Вы набираете в браузере `www.example.com`:

```
Шаг 1: Проверка кеша браузера
┌────────────────────────────────────────────────────────┐
│ Браузер проверяет свой внутренний DNS-кеш              │
│ Chrome: chrome://net-internals/#dns                    │
│ Если есть — возвращает немедленно, без запросов        │
└────────────────────────────────────────────────────────┘
                         │ Нет в кеше
                         ▼
Шаг 2: Проверка /etc/hosts (или C:\Windows\System32\drivers\etc\hosts)
┌────────────────────────────────────────────────────────┐
│ ОС проверяет локальный файл hosts                      │
│ Пример записи: 127.0.0.1  myapp.local                 │
│ Если есть — возвращает, без DNS-запросов               │
└────────────────────────────────────────────────────────┘
                         │ Нет в hosts
                         ▼
Шаг 3: Запрос к DNS Resolver (рекурсивный распознаватель)
┌────────────────────────────────────────────────────────┐
│ ОС отправляет UDP-запрос на порт 53 к resolver         │
│ (из /etc/resolv.conf или настроек DHCP)                │
│ Resolver проверяет свой кеш                            │
└────────────────────────────────────────────────────────┘
                         │ Нет в кеше resolver
                         ▼
Шаг 4: Resolver запрашивает Root Nameserver
┌────────────────────────────────────────────────────────┐
│ Resolver: «Где найти www.example.com?»                 │
│ Root: «Не знаю, но .com управляет TLD-сервер           │
│         192.5.6.30 (a.gtld-servers.net)»               │
└────────────────────────────────────────────────────────┘
                         │
                         ▼
Шаг 5: Resolver запрашивает TLD Nameserver (.com)
┌────────────────────────────────────────────────────────┐
│ Resolver: «Где найти www.example.com?»                 │
│ TLD: «Не знаю, но авторитативный NS для example.com    │
│        находится по адресу ns1.example.com»            │
└────────────────────────────────────────────────────────┘
                         │
                         ▼
Шаг 6: Resolver запрашивает Authoritative Nameserver
┌────────────────────────────────────────────────────────┐
│ Resolver: «Какой IP у www.example.com?»                │
│ Auth NS: «93.184.216.34» (финальный ответ)             │
└────────────────────────────────────────────────────────┘
                         │
                         ▼
Шаг 7: Resolver возвращает ответ клиенту и кеширует
┌────────────────────────────────────────────────────────┐
│ Resolver отправляет IP клиенту                         │
│ Кеширует результат на время TTL записи                 │
│ Браузер устанавливает TCP-соединение с 93.184.216.34  │
└────────────────────────────────────────────────────────┘
```

### Рекурсивный vs Итеративный запрос

```
Рекурсивный (клиент → resolver):
Клиент делает один запрос, resolver сам обходит все серверы
и возвращает готовый ответ.

Итеративный (resolver → root/TLD/auth):
Каждый сервер возвращает «подсказку» (referral),
кто может знать ответ. Resolver сам делает следующий запрос.
```

### TTL и кеширование

TTL (Time To Live) — время в секундах, на которое DNS-ответ должен быть закеширован:

```
Низкий TTL (60-300 сек):  Частые изменения IP, CDN, балансировка нагрузки
Средний TTL (3600 сек):   Стандартные веб-сайты
Высокий TTL (86400 сек):  Редко меняющиеся записи (MX, NS)
```

---

## 1.2.2 Типы DNS-записей

### A (Address Record)

Самый распространённый тип. Связывает доменное имя с IPv4-адресом.

```
example.com.    IN  A    93.184.216.34
www.example.com IN  A    93.184.216.34

Поля:
- Имя (Name): example.com.
- Класс (Class): IN (Internet)
- Тип: A
- Данные (RDATA): IPv4-адрес
```

Команда проверки:
```bash
dig A example.com
nslookup example.com
```

---

### AAAA (IPv6 Address Record)

Связывает имя с IPv6-адресом (4 символа «A» = 4x длиннее обычного адреса).

```
example.com.  IN  AAAA  2606:2800:220:1:248:1893:25c8:1946
```

```bash
dig AAAA google.com
```

---

### CNAME (Canonical Name Record)

Псевдоним (алиас) для другого доменного имени. CNAME указывает на другое имя, не на IP.

```
www.example.com.   IN  CNAME  example.com.
blog.example.com.  IN  CNAME  myblog.wordpress.com.
shop.example.com.  IN  CNAME  mystore.shopify.com.
```

**Важные правила CNAME:**
- CNAME не может существовать вместе с другими записями для того же имени
- CNAME не может быть у корня зоны (@) — для этого используется ALIAS/ANAME
- CNAME может указывать на другой CNAME (цепочка), но это медленно

```bash
dig CNAME www.github.com
```

---

### MX (Mail Exchange Record)

Указывает почтовые серверы для домена. Имеет приоритет (меньше = выше приоритет).

```
example.com.  IN  MX  10  mail1.example.com.
example.com.  IN  MX  20  mail2.example.com.
example.com.  IN  MX  30  mail3.example.com.

Если mail1 недоступен → пробуем mail2 → mail3
```

```bash
dig MX gmail.com
# Ответ покажет серверы Google (alt1.gmail-smtp-in.l.google.com и т.д.)
```

**Практическое применение в безопасности:**
- Проверка правильной настройки SPF, DKIM, DMARC через TXT-записи
- Поиск почтовой инфраструктуры при пентесте (OSINT)

---

### TXT (Text Record)

Произвольный текстовый контент. Используется для множества целей.

```
# SPF — разрешённые отправители почты
example.com.  IN  TXT  "v=spf1 include:_spf.google.com ~all"

# DKIM — подпись почты
selector._domainkey.example.com.  IN  TXT  "v=DKIM1; k=rsa; p=MIGfMA0..."

# DMARC — политика обработки почты
_dmarc.example.com.  IN  TXT  "v=DMARC1; p=quarantine; rua=mailto:dmarc@example.com"

# Верификация владения доменом (Google, Let's Encrypt и др.)
example.com.  IN  TXT  "google-site-verification=abc123def456"
```

```bash
dig TXT google.com
dig TXT _dmarc.google.com
```

---

### NS (Name Server Record)

Указывает авторитативные DNS-серверы для домена.

```
example.com.  IN  NS  ns1.example.com.
example.com.  IN  NS  ns2.example.com.
```

```bash
dig NS example.com
```

**В безопасности:** При пентесте NS-записи помогают найти хостинг-провайдера и дополнительные цели.

---

### PTR (Pointer Record / Reverse DNS)

Обратный DNS: IP-адрес → доменное имя. Записывается в специальной зоне `.in-addr.arpa`.

```
# Прямой DNS: example.com → 93.184.216.34
# Обратный DNS: 93.184.216.34 → example.com

34.216.184.93.in-addr.arpa.  IN  PTR  example.com.
# IP записывается в обратном порядке!
```

```bash
dig -x 8.8.8.8
# Результат: 8.8.8.8.in-addr.arpa → dns.google
nslookup 8.8.8.8
```

**В безопасности:** Отсутствие PTR или несоответствие PTR и A-записи — признак спама/фишинга.

---

### SOA (Start of Authority)

Содержит административную информацию о DNS-зоне. Есть в каждой зоне.

```
example.com.  IN  SOA  ns1.example.com. admin.example.com. (
                        2024010101  ; Serial (версия зоны)
                        3600        ; Refresh (как часто NS обновляются)
                        900         ; Retry (при ошибке обновления)
                        604800      ; Expire (когда данные устаревают)
                        86400       ; Minimum TTL
                       )
```

```bash
dig SOA example.com
```

---

## 1.2.3 DNS как вектор атаки

### DNS Spoofing / Cache Poisoning

**Принцип:** Злоумышленник подменяет DNS-ответ, заставляя resolver закешировать неверный IP.

```
Нормальный сценарий:
Клиент → Resolver → Auth NS → «bank.com = 1.2.3.4»
Клиент → 1.2.3.4 (настоящий банк)

Атака:
Клиент → Resolver → (злоумышленник перехватывает) → «bank.com = 5.6.7.8»
Клиент → 5.6.7.8 (фишинговый сайт!)
```

**Классическая атака Каминского (2008):**
- Атакующий заставляет resolver делать тысячи запросов
- Параллельно бомбардирует поддельными ответами с угадыванием 16-битного ID транзакции
- Вероятность угадать: 1/65536 за попытку

**Защита:**
- DNSSEC (подпись ответов криптографически)
- DNS over HTTPS (DoH) — шифрование DNS-трафика
- DNS over TLS (DoT)
- Рандомизация исходного порта (source port randomization)

---

### DNS Tunneling

**Принцип:** Использование DNS-запросов для передачи произвольных данных через корпоративный файрвол.

```
Злоумышленник контролирует NS для evil.com
Заражённая машина внутри корпоративной сети:

Данные для exfiltration:
  "secret_data_chunk1" → base64 → "c2VjcmV0X2RhdGFfY2h1bmsx"

DNS-запрос: c2VjcmV0X2RhdGFfY2h1bmsxcGFydDE.evil.com A ?
             ↑ данные закодированы в поддомене ↑

Ответ DNS: NS-сервер evil.com декодирует данные и отвечает командой
```

**Инструменты:** dnscat2, iodine, DNSExfiltrator

**Обнаружение:**
- Аномально длинные DNS-запросы (>50 символов субдомена)
- Высокая частота DNS-запросов к одному домену
- Запросы с высокой энтропией (base64 выглядит как случайные символы)
- Необычные типы DNS-записей (TXT, NULL) в ответах

```bash
# Обнаружение DNS tunneling в Wireshark:
# Фильтр: dns && frame.len > 150
# Или: dns.qry.name matches "[a-zA-Z0-9+/=]{30,}"
```

---

### DNS Amplification (DRDoS)

**Принцип:** Атака с усилением — маленький запрос генерирует большой ответ, направленный на жертву.

```
Коэффициент усиления: до 50-100x

Злоумышленник (IP жертвы):          Open Resolver:
UDP: SRC=жертва, DST=resolver       
  ANY google.com? (40 байт) ──────► 
                              ◄──── Ответ 3000+ байт → отправляется жертве!

Злоумышленник отправил 40 байт → жертва получила 3000 байт (усиление 75x)
```

**Запросы с максимальным усилением:**
- `ANY` (ANY record) — максимальный размер ответа
- `TXT` — для доменов с длинными SPF/DMARC записями

**Защита:**
- Блокировка open resolvers (resolver должен отвечать только своим клиентам)
- Response Rate Limiting (RRL) на DNS-серверах
- BCP38 — фильтрация пакетов с поддельными IP (anti-spoofing)

---

## 1.2.4 HTTP методы

HTTP (HyperText Transfer Protocol) — протокол прикладного уровня для передачи гипертекста. HTTP/1.1, HTTP/2, HTTP/3 — основные версии.

### GET

Запрашивает ресурс. **Идемпотентный** (повторный запрос даёт тот же результат). Данные передаются в URL.

```http
GET /users?page=1&limit=10 HTTP/1.1
Host: api.example.com
Accept: application/json
```

**Особенности:**
- Данные в URL (видны в логах, браузерной истории, Referer)
- Кешируется браузером и прокси
- Ограничение длины URL (~2000-8000 символов)
- Не используется для передачи чувствительных данных

---

### POST

Отправляет данные для создания ресурса. **НЕ идемпотентный** (повторный запрос создаёт новую сущность).

```http
POST /users HTTP/1.1
Host: api.example.com
Content-Type: application/json
Content-Length: 45

{"name": "Alice", "email": "alice@example.com"}
```

**Особенности:**
- Данные в теле запроса (не видны в URL)
- Не кешируется
- Нет ограничения на размер данных
- Используется для форм входа, создания ресурсов, загрузки файлов

---

### PUT

Полностью заменяет ресурс. **Идемпотентный**.

```http
PUT /users/42 HTTP/1.1
Host: api.example.com
Content-Type: application/json

{"name": "Alice Smith", "email": "alice.smith@example.com", "age": 30}
```

Если ресурс не существует — создаёт. При повторном вызове — тот же результат.

---

### PATCH

Частично обновляет ресурс. Может быть НЕ идемпотентным (зависит от реализации).

```http
PATCH /users/42 HTTP/1.1
Host: api.example.com
Content-Type: application/json

{"email": "newemail@example.com"}
```

Только указанные поля изменяются, остальные остаются.

---

### DELETE

Удаляет ресурс. **Идемпотентный** (повторное удаление — тот же результат: ресурс уже удалён).

```http
DELETE /users/42 HTTP/1.1
Host: api.example.com
Authorization: Bearer eyJ0eXAiOiJKV1QiLCJhbGciOiJIUzI1NiJ9...
```

---

### OPTIONS

Запрашивает разрешённые методы для ресурса. Используется для CORS preflight-запросов.

```http
OPTIONS /api/users HTTP/1.1
Host: api.example.com
Origin: https://frontend.example.com
Access-Control-Request-Method: POST
Access-Control-Request-Headers: Content-Type, Authorization
```

Ответ сервера:
```http
HTTP/1.1 204 No Content
Allow: GET, POST, PUT, DELETE, OPTIONS
Access-Control-Allow-Origin: https://frontend.example.com
Access-Control-Allow-Methods: GET, POST, PUT, DELETE
Access-Control-Allow-Headers: Content-Type, Authorization
```

---

### HEAD

Идентичен GET, но возвращает только заголовки (без тела ответа). Используется для проверки существования ресурса или кеширования.

```http
HEAD /large-file.iso HTTP/1.1
Host: downloads.example.com
```

Ответ содержит `Content-Length`, `Last-Modified`, `ETag` — без самого файла. Позволяет узнать размер файла перед скачиванием.

---

### Таблица методов

```
Метод    Идемпотентный  Безопасный  Тело запроса  Применение
────────────────────────────────────────────────────────────────────
GET      Да             Да          Нет           Получение данных
HEAD     Да             Да          Нет           Метаданные
POST     Нет            Нет         Да            Создание, действия
PUT      Да             Нет         Да            Полное обновление
PATCH    Нет*           Нет         Да            Частичное обновление
DELETE   Да             Нет         Нет/Да        Удаление
OPTIONS  Да             Да          Нет           CORS preflight
```

**Безопасный метод** — не изменяет состояние сервера.

---

## 1.2.5 HTTP коды ответов

HTTP-коды состоят из трёх цифр. Первая цифра указывает класс:

### 1xx — Информационные (Informational)

Запрос получен, обработка продолжается.

```
100 Continue        — Сервер получил заголовки, можно отправлять тело
101 Switching Protocols — WebSocket upgrade
```

### 2xx — Успех (Success)

Запрос успешно получен, понят и обработан.

```
200 OK              — Стандартный успешный ответ
201 Created         — Ресурс создан (POST, PUT)
204 No Content      — Успешно, но нет тела ответа (DELETE, OPTIONS)
206 Partial Content — Частичный контент (для range-запросов, download resume)
```

### 3xx — Перенаправление (Redirection)

Клиент должен предпринять дополнительные действия.

```
301 Moved Permanently  — Постоянный редирект (SEO: меняет ссылки)
302 Found              — Временный редирект
304 Not Modified       — Кеш актуален, тело не передаётся
307 Temporary Redirect — Временный редирект, метод не меняется
308 Permanent Redirect — Постоянный редирект, метод не меняется
```

**Важно для безопасности:** Open Redirect — если приложение перенаправляет на произвольный URL из параметра:
```
http://example.com/login?redirect=https://evil.com
```

### 4xx — Ошибки клиента (Client Errors)

Клиент допустил ошибку в запросе.

```
400 Bad Request        — Некорректный синтаксис запроса
401 Unauthorized       — Требуется аутентификация (нет или неверные credentials)
403 Forbidden          — Доступ запрещён (аутентификация есть, но прав нет)
404 Not Found          — Ресурс не найден
405 Method Not Allowed — Метод не поддерживается
408 Request Timeout    — Клиент слишком долго отправлял запрос
409 Conflict           — Конфликт с текущим состоянием ресурса
410 Gone               — Ресурс удалён навсегда
413 Payload Too Large  — Тело запроса слишком большое
415 Unsupported Media Type — Неподдерживаемый Content-Type
422 Unprocessable Entity   — Данные понятны, но семантически неверны
429 Too Many Requests  — Превышен лимит запросов (rate limiting)
```

**Разница 401 vs 403:**
- `401` — «Я тебя не знаю. Представься.»
- `403` — «Я тебя знаю, но тебе сюда нельзя.»

### 5xx — Ошибки сервера (Server Errors)

Сервер не смог выполнить корректный запрос.

```
500 Internal Server Error  — Общая ошибка сервера (часто раскрывает stack trace!)
501 Not Implemented        — Метод не реализован
502 Bad Gateway            — Плохой ответ от upstream (nginx → PHP-FPM упал)
503 Service Unavailable    — Сервис недоступен (перегрузка, обслуживание)
504 Gateway Timeout        — Upstream не ответил вовремя
```

**Безопасность:** `500 Internal Server Error` с подробным stack trace раскрывает:
- Язык программирования, версию фреймворка
- Пути файлов на сервере
- SQL-запросы (при SQL-ошибках)
- Переменные окружения

---

## 1.2.6 HTTP заголовки

### Заголовки запроса (Request Headers)

```http
GET /api/profile HTTP/1.1
Host: api.example.com
User-Agent: Mozilla/5.0 (X11; Linux x86_64) Chrome/120.0.0.0
Accept: application/json, text/plain, */*
Accept-Language: ru-RU,ru;q=0.9,en-US;q=0.8
Accept-Encoding: gzip, deflate, br
Authorization: Bearer eyJhbGciOiJIUzI1NiIsInR5cCI6IkpXVCJ9...
Cookie: session_id=abc123; _ga=GA1.2.123456789.1234567890
Referer: https://example.com/dashboard
Content-Type: application/json
Content-Length: 0
Connection: keep-alive
X-Requested-With: XMLHttpRequest
X-Forwarded-For: 203.0.113.42
```

**Host** — обязательный заголовок в HTTP/1.1. Позволяет одному IP обслуживать несколько доменов (виртуальный хостинг). **Атака:** Host Header Injection может привести к password reset poisoning.

**User-Agent** — идентификатор клиента. Содержит браузер, ОС, версию. **В безопасности:** изменение User-Agent помогает обойти примитивные фильтры по User-Agent.

**Authorization** — передача credentials:
```
Basic: "Authorization: Basic dXNlcjpwYXNz" (base64 от "user:pass")
Bearer: "Authorization: Bearer <JWT-токен>"
API Key: "Authorization: ApiKey abc123"
```

**Cookie** — сессионные данные. **Атака:** кража cookie через XSS, CSRF.

**Referer** — откуда пришёл пользователь. **Безопасность:** не используйте Referer для авторизации — он может быть подделан или отсутствовать.

**X-Forwarded-For** — IP клиента за прокси/балансировщиком. **Атака:** подделка XFF для обхода IP-based ограничений:
```bash
curl -H "X-Forwarded-For: 127.0.0.1" https://api.example.com/admin
```

---

### Заголовки ответа (Response Headers)

```http
HTTP/1.1 200 OK
Content-Type: application/json; charset=utf-8
Content-Length: 1234
Content-Encoding: gzip
Cache-Control: no-store, no-cache, must-revalidate
Set-Cookie: session_id=xyz789; HttpOnly; Secure; SameSite=Strict; Max-Age=3600
Location: https://example.com/new-url
Server: nginx/1.24.0
X-Content-Type-Options: nosniff
X-Frame-Options: DENY
Strict-Transport-Security: max-age=31536000; includeSubDomains
Content-Security-Policy: default-src 'self'; script-src 'self' 'nonce-abc123'
X-XSS-Protection: 1; mode=block
Access-Control-Allow-Origin: https://frontend.example.com
ETag: "686897696a7c876b7e"
Last-Modified: Mon, 01 Jan 2024 00:00:00 GMT
```

**Set-Cookie атрибуты:**
- `HttpOnly` — cookie недоступна через JavaScript (защита от XSS)
- `Secure` — передаётся только по HTTPS
- `SameSite=Strict` — cookie не отправляется в cross-site запросах (защита от CSRF)
- `SameSite=Lax` — отправляется при навигации (GET-запросы), но не при AJAX
- `SameSite=None; Secure` — для сторонних cookie (требует Secure)

**Заголовки безопасности:**
- `X-Content-Type-Options: nosniff` — запрет MIME-sniffing (браузер не угадывает тип)
- `X-Frame-Options: DENY` — запрет встраивания в iframe (защита от clickjacking)
- `HSTS` — принудительный HTTPS (браузер не будет делать HTTP-запросы)
- `CSP` — контроль источников контента (защита от XSS)

**Server** — раскрывает веб-сервер и версию. **Рекомендация:** скрывать или менять на нейтральное значение.

---

### CORS (Cross-Origin Resource Sharing)

CORS — механизм, позволяющий веб-страницам делать запросы к другому домену.

```
Источник (Origin) = Схема + Домен + Порт
https://app.example.com:443

Cross-origin запрос (из браузера):
https://frontend.example.com → https://api.example.com

Same-origin:
https://example.com/page1 → https://example.com/api (разрешено всегда)
```

**CORS Preflight:**
```
1. Браузер видит cross-origin запрос с нестандартным методом/заголовком
2. Автоматически отправляет OPTIONS preflight
3. Сервер отвечает, что разрешено
4. Браузер выполняет реальный запрос
```

**Уязвимость CORS:**
```http
# Опасная конфигурация:
Access-Control-Allow-Origin: *
Access-Control-Allow-Credentials: true
# Это невозможно! Браузер не допускает * + credentials

# Ещё опасная:
Origin: https://evil.com
→ Access-Control-Allow-Origin: https://evil.com  (зеркалирование Origin без проверки)
```

---

## 1.2.7 HTTPS и TLS

### Зачем нужен HTTPS?

HTTP передаёт данные в открытом виде. HTTPS добавляет:
- **Конфиденциальность** — шифрование данных
- **Целостность** — данные не изменены при передаче
- **Аутентификацию** — подтверждение подлинности сервера (через сертификат)

### TLS Handshake (шаг за шагом)

TLS 1.3 (современный стандарт):

```
Клиент                                          Сервер
  │                                               │
  │  ClientHello                                  │
  │  - TLS версия                                 │
  │  - Случайное число (client_random)            │
  │  - Список поддерживаемых шифров               │
  │  - SNI (Server Name Indication)               │
  │  - Поддерживаемые группы Diffie-Hellman       │
  │ ─────────────────────────────────────────────►│
  │                                               │
  │                              ServerHello      │
  │                  - Выбранный шифр             │
  │                  - server_random              │
  │                  - Certificate (X.509)        │
  │                  - CertificateVerify          │
  │                  - Finished (MAC)             │
  │ ◄─────────────────────────────────────────── │
  │                                               │
  │ [Клиент проверяет сертификат]                 │
  │ - Подпись CA                                  │
  │ - Срок действия                               │
  │ - Имя домена (CN/SAN)                         │
  │ - Не отозван (OCSP/CRL)                       │
  │                                               │
  │  Finished (MAC)                               │
  │ ─────────────────────────────────────────────►│
  │                                               │
  │  ════ Зашифрованные данные ════               │
  │ ◄────────────────────────────────────────────►│
```

### X.509 Сертификаты

Структура сертификата:

```
Certificate:
    Version: 3 (0x2)
    Serial Number: 04:00:00:00:00:01:15:4b:5a:c3:94
    Signature Algorithm: sha256WithRSAEncryption
    Issuer: C=US, O=DigiCert Inc, CN=DigiCert SHA2 Secure Server CA
    Validity:
        Not Before: Jan 1 00:00:00 2024 GMT
        Not After : Jan 1 00:00:00 2025 GMT
    Subject: C=US, ST=California, L=Mountain View, O=Google LLC, CN=*.google.com
    Subject Public Key Info:
        Public Key Algorithm: id-ecPublicKey
        Public-Key: (256 bit)
    X509v3 Extensions:
        Subject Alternative Name:
            DNS:*.google.com, DNS:google.com
        Key Usage: Digital Signature
        Extended Key Usage: TLS Web Server Authentication
        CRL Distribution Points: http://crl.digicert.com/sha2-ha-server-g6.crl
        Authority Information Access:
            OCSP: http://ocsp.digicert.com
```

**Цепочка доверия:**
```
Root CA (встроен в ОС/браузер)
  └─ Intermediate CA (подписан Root CA)
       └─ Server Certificate (подписан Intermediate CA)
```

**Типы сертификатов:**
- DV (Domain Validation) — только проверка владения доменом (Let's Encrypt)
- OV (Organization Validation) — проверка организации
- EV (Extended Validation) — расширенная проверка (зелёный замок с именем компании)
- Wildcard `*.example.com` — для всех поддоменов одного уровня
- SAN (Subject Alternative Names) — несколько доменов в одном сертификате

### SNI (Server Name Indication)

Проблема: один IP может обслуживать несколько HTTPS-сайтов. Но TLS handshake происходит до передачи Host-заголовка HTTP.

**Решение SNI:** Клиент в ClientHello сообщает имя хоста (SNI extension), сервер выбирает правильный сертификат.

```
ClientHello:
  server_name: www.example.com  ← SNI
```

**Последствие для приватности:** SNI передаётся в открытом виде → наблюдатель видит, к какому сайту вы обращаетесь, даже через HTTPS. Решение: ECH (Encrypted Client Hello) в TLS 1.3.

---

## 💻 Практика

### Задание 1: Исследование DNS с dig

```bash
# Установка
sudo apt-get install dnsutils

# Простой A-запрос
dig google.com

# Получить только IP (краткий вывод)
dig +short google.com

# Запросить конкретный тип записи
dig MX gmail.com
dig TXT google.com
dig NS google.com
dig AAAA google.com

# Трассировка DNS-запроса (видим весь путь: root → TLD → authoritative)
dig +trace google.com

# Запросить конкретный сервер (обойти локальный resolver)
dig @8.8.8.8 google.com
dig @1.1.1.1 google.com

# Обратный DNS
dig -x 8.8.8.8

# Получить все записи
dig ANY google.com

# Запрос с отладочной информацией
dig +stats +question google.com
```

### Задание 2: nslookup для Windows и кросс-платформенно

```bash
# Интерактивный режим
nslookup
> server 8.8.8.8
> google.com
> set type=MX
> gmail.com
> exit

# Прямой запрос
nslookup google.com
nslookup google.com 8.8.8.8

# Запрос конкретного типа
nslookup -type=MX gmail.com
nslookup -type=TXT google.com
nslookup -type=NS google.com
```

### Задание 3: Анализ HTTP с curl

```bash
# Базовый запрос с подробным выводом (-v показывает заголовки)
curl -v http://httpbin.org/get

# GET-запрос с заголовками
curl -H "Accept: application/json" \
     -H "X-Custom-Header: test" \
     https://httpbin.org/get

# POST-запрос с JSON
curl -X POST \
     -H "Content-Type: application/json" \
     -d '{"username": "alice", "password": "secret"}' \
     https://httpbin.org/post

# Следовать редиректам
curl -L http://google.com

# Сохранить заголовки ответа
curl -I https://google.com         # только заголовки (HEAD)
curl -D headers.txt https://google.com  # тело + заголовки в файл

# Игнорировать ошибки сертификата (для тестирования)
curl -k https://expired.badssl.com

# Использовать прокси
curl -x http://proxy:8080 https://example.com

# Отправить Cookie
curl -b "session_id=abc123; token=xyz" https://example.com

# Измерить время запроса
curl -w "DNS: %{time_namelookup}s\nConnect: %{time_connect}s\nTTFB: %{time_starttransfer}s\nTotal: %{time_total}s\n" \
     -o /dev/null -s https://google.com
```

### Задание 4: Захват DNS и HTTP в Wireshark

```bash
# Запустить захват на интерфейсе
sudo wireshark &
# Или tcpdump для захвата:
sudo tcpdump -i eth0 -w capture.pcap port 53 or port 80

# Сгенерировать трафик в другом терминале:
dig +trace example.com
curl -v http://httpbin.org/get

# Фильтры Wireshark для DNS:
# dns
# dns.qry.name == "google.com"
# dns.resp.addr == "8.8.8.8"

# Фильтры для HTTP:
# http
# http.request.method == "GET"
# http.response.code == 200
# http.host == "httpbin.org"
```

---

## 🛠️ Задания

### ⭐ Задание 1 (Базовый уровень): DNS-разведка домена

Выберите любой популярный сайт (например, `vk.com` или `yandex.ru`) и с помощью `dig` найдите:

1. A-записи (IPv4-адреса)
2. AAAA-записи (IPv6-адреса, если есть)
3. MX-записи (почтовые серверы)
4. NS-записи (авторитативные серверы)
5. TXT-записи (SPF, DKIM, DMARC)
6. Сделайте `dig +trace` и опишите каждый шаг

Оформите отчёт: какой хостинг использует сайт? Какой почтовый провайдер?

---

### ⭐⭐ Задание 2 (Средний уровень): Анализ HTTP-трафика

1. Используя `curl -v`, выполните:
   - GET-запрос к `https://httpbin.org/get`
   - POST-запрос к `https://httpbin.org/post` с JSON-телом
   - Запрос с кастомными заголовками

2. Для каждого запроса задокументируйте:
   - Все заголовки запроса
   - Все заголовки ответа
   - HTTP код ответа
   - Время выполнения (с флагом `-w`)

3. Найдите сайт без HTTPS (или используйте `http://httpbin.org`) и:
   - Захватите запрос в Wireshark
   - Покажите, что данные передаются в открытом виде
   - Докажите, почему это опасно (что видит атакующий в той же сети)

---

### ⭐⭐⭐ Задание 3 (Продвинутый уровень): Симуляция DNS-атаки

> **Только в изолированной лабораторной среде!**

1. Настройте локальную DNS-зону с помощью `dnsmasq`:
```bash
sudo apt-get install dnsmasq
# Добавьте в /etc/dnsmasq.conf:
# address=/evil-bank.com/127.0.0.1
sudo systemctl restart dnsmasq
```

2. Измените `/etc/resolv.conf` для использования локального DNS:
```bash
nameserver 127.0.0.1
```

3. Проверьте, что DNS возвращает ваш IP для поддельного домена:
```bash
dig evil-bank.com @127.0.0.1
```

4. Напишите отчёт:
   - Как работает DNS cache poisoning (объясните атаку Каминского)
   - Какие меры защиты предотвращают такие атаки (DNSSEC, DoH, DoT)
   - Как DNSSEC подписывает записи (объясните цепочку валидации)

---

## ✅ Чеклист готовности

- [ ] Я могу объяснить весь путь DNS-запроса от браузера до авторитативного сервера
- [ ] Я знаю назначение каждого типа DNS-записей: A, AAAA, CNAME, MX, TXT, NS, PTR, SOA
- [ ] Я могу выполнить DNS-разведку с помощью `dig` и `nslookup`
- [ ] Я понимаю три основных DNS-атаки: spoofing, tunneling, amplification
- [ ] Я знаю все основные HTTP-методы и понимаю разницу между идемпотентными и нет
- [ ] Я могу объяснить код ответа 401 vs 403, 301 vs 302, 502 vs 503
- [ ] Я понимаю, как работают заголовки Set-Cookie (HttpOnly, Secure, SameSite)
- [ ] Я знаю заголовки безопасности: HSTS, CSP, X-Frame-Options, X-Content-Type-Options
- [ ] Я могу объяснить TLS handshake пошагово
- [ ] Я понимаю, зачем нужен SNI и какова его роль в приватности
- [ ] Я умею анализировать HTTP-трафик с помощью `curl -v` и Wireshark

---

## 🔗 Ресурсы

### Спецификации и RFC
- [RFC 1034/1035 — Domain Names](https://www.rfc-editor.org/rfc/rfc1035)
- [RFC 7230-7235 — HTTP/1.1](https://www.rfc-editor.org/rfc/rfc7230)
- [RFC 8446 — TLS 1.3](https://www.rfc-editor.org/rfc/rfc8446)
- [RFC 6797 — HSTS](https://www.rfc-editor.org/rfc/rfc6797)
- [RFC 7540 — HTTP/2](https://www.rfc-editor.org/rfc/rfc7540)

### Интерактивные инструменты
- [DNSdumpster — DNS разведка](https://dnsdumpster.com)
- [MXToolbox — диагностика DNS и почты](https://mxtoolbox.com)
- [SecurityHeaders.com — анализ HTTP-заголовков](https://securityheaders.com)
- [SSL Labs — анализ TLS-конфигурации](https://www.ssllabs.com/ssltest/)
- [httpbin.org — тестирование HTTP](https://httpbin.org)
- [BadSSL — тестирование HTTPS-ошибок](https://badssl.com)

### Визуальные объяснения
- [How DNS Works (comic)](https://howdns.works)
- [TLS 1.3 Handshake визуализация](https://tls13.xargs.org)
- [HTTP Statuses — httpstatuses.com](https://httpstatuses.com)

### Дополнительное чтение
- [OWASP Transport Layer Security Cheat Sheet](https://cheatsheetseries.owasp.org/cheatsheets/Transport_Layer_Security_Cheat_Sheet.html)
- [Cloudflare — What is DNS?](https://www.cloudflare.com/learning/dns/what-is-dns/)
- [Mozilla HTTP Headers Reference](https://developer.mozilla.org/en-US/docs/Web/HTTP/Headers)
