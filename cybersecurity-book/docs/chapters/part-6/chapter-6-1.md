# Глава 6.1: Что такое SIEM — архитектура и принципы работы

## 🎯 Цели главы

- Понять, что такое SIEM и зачем он нужен в корпоративной безопасности
- Изучить архитектуру SIEM: от источников данных до алертов и дашбордов
- Познакомиться с популярными SIEM-решениями на рынке (коммерческими и open-source)
- Освоить ключевые метрики и термины SIEM-аналитика
- Развернуть собственный SIEM (Wazuh All-in-One) в домашней лаборатории

---

## 1. Что такое SIEM и зачем он нужен

### Определение

**SIEM** (Security Information and Event Management — Управление информацией и событиями безопасности) — это класс программных решений, которые:

1. **Собирают** события безопасности из множества источников в реальном времени
2. **Нормализуют** разнородные форматы в единый стандарт
3. **Коррелируют** события между собой для выявления паттернов атак
4. **Алертируют** аналитиков при срабатывании правил
5. **Хранят** логи для расследований и соответствия требованиям регуляторов

Термин объединяет два исторически разных класса:
- **SIM** (Security Information Management) — долгосрочное хранение и отчётность
- **SEM** (Security Event Management) — анализ в реальном времени и алертинг

### Проблема, которую решает SIEM

Представьте типичную корпоративную сеть среднего размера:

| Источник логов | Количество событий в сутки |
|---|---|
| Контроллер домена (Windows AD) | 500 000 — 2 000 000 |
| Файрвол (периметр) | 1 000 000 — 10 000 000 |
| Веб-прокси | 200 000 — 500 000 |
| Антивирус на 300 хостах | 50 000 — 300 000 |
| VPN-шлюз | 10 000 — 100 000 |
| Почтовый шлюз | 50 000 — 200 000 |

**Итого: 2–13 миллионов событий в сутки.**

Проблемы без SIEM:
- Аналитик физически не может просматривать миллионы строк
- Логи хранятся в разных форматах (Windows XML, Syslog, CEF, JSON)
- Логи разбросаны по десяткам серверов
- Атака, затрагивающая 3 разных системы, невидима в каждой из них по отдельности
- Нет уведомлений: атаку замечают спустя недели или месяцы (MTTD — Mean Time To Detect)

**SIEM решает все эти проблемы централизованно.**

---

## 2. Архитектура SIEM

Архитектура SIEM — это конвейер от источника данных до действия аналитика. Рассмотрим каждый компонент.

```
┌─────────────────────────────────────────────────────────────────┐
│                        ИСТОЧНИКИ ДАННЫХ                         │
│  Windows Logs │ Syslog │ Firewall │ IDS/IPS │ Proxy │ EDR       │
└──────────────────────────┬──────────────────────────────────────┘
                           │
                    ┌──────▼──────┐
                    │   АГЕНТЫ   │
                    │ Beats/Wazuh │
                    └──────┬──────┘
                           │
                    ┌──────▼──────┐
                    │ НОРМАЛИЗАЦИЯ│
                    │ CEF/LEEF/   │
                    │ Единый формат│
                    └──────┬──────┘
                           │
                    ┌──────▼──────┐
                    │  ХРАНЕНИЕ  │
                    │Elasticsearch│
                    │  Индексы   │
                    └──────┬──────┘
                           │
                    ┌──────▼──────┐
                    │ КОРРЕЛЯЦИЯ │
                    │  Правила   │
                    │  Движок    │
                    └──────┬──────┘
                           │
                    ┌──────▼──────┐
                    │  АЛЕРТИНГ  │
                    │ Тикеты/Email│
                    │  Webhook   │
                    └──────┬──────┘
                           │
                    ┌──────▼──────┐
                    │ ДАШБОРДЫ   │
                    │Kibana/Splunk│
                    └─────────────┘
```

### 2.1 Источники данных

#### Windows Event Logs
Центральный источник для Windows-инфраструктуры. Основные каналы:
- **Security** — аутентификация (4624, 4625), привилегии (4672), аудит объектов
- **System** — события ОС, установка сервисов (7045)
- **Application** — события приложений
- **Sysmon** — расширенный мониторинг: создание процессов (1), сетевые соединения (3), загрузка драйверов (6), создание файлов (11)

Важнейшие Event ID для SOC:

| Event ID | Описание | Значение для безопасности |
|---|---|---|
| 4624 | Успешный вход | Lateral movement, необычное время/IP |
| 4625 | Неудачный вход | Брутфорс |
| 4648 | Вход с явными учётными данными | Pass-the-Hash, Runas |
| 4672 | Вход привилегированного аккаунта | Privilege escalation |
| 4698 | Создание задачи планировщика | Persistence |
| 4720 | Создание учётной записи | Backdoor account |
| 4732 | Добавление пользователя в группу | Privilege escalation |
| 7045 | Установка нового сервиса | Malware persistence |
| 4104 | PowerShell Script Block Logging | Powershell-атаки |

#### Syslog (Linux/Unix/сетевые устройства)
Стандартный протокол для Linux, macOS, маршрутизаторов, коммутаторов, файрволов. Порт UDP/TCP 514 (зашифрованный TLS — 6514).

Формат сообщения:
```
<Priority>Timestamp Hostname Process[PID]: Message
<134>Feb 24 10:15:32 webserver01 sshd[1234]: Failed password for root from 203.0.113.45 port 52341 ssh2
```

Priority = Facility × 8 + Severity. Severity: 0=Emergency, 1=Alert, 2=Critical, 3=Error, 4=Warning, 5=Notice, 6=Info, 7=Debug.

#### Firewall Logs
Каждый файрвол имеет свой формат, но общие поля:
- `src_ip`, `dst_ip`, `src_port`, `dst_port`
- `protocol` (TCP/UDP/ICMP)
- `action` (ALLOW/DENY/DROP)
- `bytes_sent`, `bytes_recv`
- `duration`, `timestamp`

#### IDS/IPS
Suricata, Snort, Zeek генерируют события при срабатывании сигнатур. Ключевые поля: `rule_name`, `signature_id`, `severity`, `src_ip`, `dst_ip`, `category`.

#### Proxy Logs
Squid, Blue Coat, Zscaler: какой пользователь обратился к какому URL, статус, байты. Критично для обнаружения C2-коммуникации и data exfiltration.

#### EDR (Endpoint Detection & Response)
CrowdStrike, Carbon Black, Defender for Endpoint, Wazuh — телеметрия с уровня хоста: процессы, файлы, реестр, сеть. Богатейший источник для расследований.

### 2.2 Агенты сбора данных

Агент — программа, устанавливаемая на хост для сбора и отправки логов в SIEM.

#### Beats (Elastic Stack)
Семейство лёгких агентов от Elastic:

| Beat | Назначение |
|---|---|
| **Filebeat** | Файлы логов (syslog, nginx, apache, custom) |
| **Winlogbeat** | Windows Event Log |
| **Auditbeat** | Linux audit framework |
| **Packetbeat** | Сетевой трафик (DNS, HTTP, MySQL) |
| **Metricbeat** | Системные метрики (CPU, RAM, disk) |
| **Heartbeat** | Мониторинг доступности сервисов |

Принцип работы Filebeat:
```yaml
# filebeat.yml
filebeat.inputs:
  - type: log
    enabled: true
    paths:
      - /var/log/nginx/access.log
      - /var/log/nginx/error.log
    fields:
      service: nginx
      environment: production

output.elasticsearch:
  hosts: ["https://elasticsearch:9200"]
  ssl.certificate_authorities: ["/etc/pki/root/ca.crt"]
```

#### Wazuh Agent
Мощный open-source агент, совмещающий функции:
- Сбор и пересылка логов (как Filebeat)
- **HIDS** (Host Intrusion Detection System)
- **FIM** (File Integrity Monitoring) — контроль изменений файлов
- Обнаружение руткитов
- Мониторинг уязвимостей (SCA — Security Configuration Assessment)
- Активный ответ (автоматическая блокировка при инциденте)

#### Splunk Universal Forwarder (UF)
Лёгкий агент Splunk (~30 МБ), устанавливается на источник данных, пересылает данные на Heavy Forwarder или Indexer. Минимальная нагрузка на хост.

### 2.3 Нормализация

**Проблема:** каждый источник имеет свой формат. Пример для события "неудачный вход":

Windows Event Log (XML):
```xml
<EventData>
  <Data Name="SubjectUserName">SYSTEM</Data>
  <Data Name="TargetUserName">administrator</Data>
  <Data Name="IpAddress">203.0.113.45</Data>
  <Data Name="Status">0xC000006D</Data>
</EventData>
```

Linux syslog:
```
Failed password for administrator from 203.0.113.45 port 52341 ssh2
```

Cisco ASA Firewall:
```
%ASA-6-302013: Built inbound TCP connection 12345 for outside:203.0.113.45/52341 to inside:10.0.0.1/22
```

**Нормализация** — приведение всех этих форматов к единой схеме полей:

| Единое поле | Описание |
|---|---|
| `src_ip` | IP источника атаки |
| `dst_ip` | IP цели |
| `user` | Целевой пользователь |
| `action` | failed_login / success_login |
| `event_type` | authentication |
| `timestamp` | ISO 8601 |

#### Основные форматы SIEM

**CEF (Common Event Format)** — стандарт ArcSight/Micro Focus:
```
CEF:0|Microsoft|Windows|10.0|4625|An account failed to log on|3|src=203.0.113.45 suser=administrator outcome=failure
```

**LEEF (Log Event Extended Format)** — стандарт IBM QRadar:
```
LEEF:2.0|Microsoft|Windows|10.0|4625|src=203.0.113.45  usrName=administrator
```

**ECS (Elastic Common Schema)** — стандарт Elastic Stack:
```json
{
  "event.category": "authentication",
  "event.action": "logon-failed",
  "source.ip": "203.0.113.45",
  "user.name": "administrator",
  "@timestamp": "2024-02-24T10:15:32.000Z"
}
```

#### Grok-паттерны в Logstash

Logstash использует grok для разбора неструктурированных строк:
```ruby
filter {
  grok {
    match => {
      "message" => "%{SYSLOGTIMESTAMP:timestamp} %{HOSTNAME:host} sshd\[%{NUMBER:pid}\]: Failed password for %{USERNAME:user} from %{IP:src_ip} port %{NUMBER:src_port}"
    }
  }
  mutate {
    add_field => { "event_type" => "failed_login" }
  }
}
```

### 2.4 Хранение

#### Индексирование
Elasticsearch хранит данные в индексах. Каждый документ индексируется — все поля доступны для быстрого поиска.

Типичная схема индексов по времени:
```
logs-2024.02.01
logs-2024.02.02
...
logs-2024.02.24
```

Index Lifecycle Management (ILM) автоматически переводит старые индексы:
- **Hot** → активный, быстрый поиск
- **Warm** → менее частый доступ, оптимизирован для хранения
- **Cold** → редкий доступ
- **Delete** → удаление по истечении retention

#### Retention Policy (политика хранения)
Сколько хранить логи:

| Тип данных | Рекомендуемый срок |
|---|---|
| Security events | 12 месяцев |
| Firewall logs | 6–12 месяцев |
| DNS logs | 3–6 месяцев |
| Proxy logs | 3–6 месяцев |
| PCI DSS (требование) | 12 месяцев |
| GDPR (EU) | минимально необходимый срок |
| Российское законодательство (149-ФЗ) | 3 года для операторов КИИ |

### 2.5 Корреляция

**Корреляция** — обнаружение паттернов атаки путём связывания нескольких событий.

#### Типы корреляции:

**1. Простое правило (одно событие):**
```
IF event.category = "malware_detection" THEN alert(HIGH)
```

**2. Счётчик (threshold):**
```
IF count(event_type="failed_login", src_ip=X) > 10
   WITHIN 5 minutes
THEN alert("Brute Force", severity=HIGH)
```

**3. Последовательность (sequence/chaining):**
```
IF event A (failed_login × 10) FOLLOWED BY
   event B (success_login from same IP) WITHIN 30 minutes
THEN alert("Brute Force Success", severity=CRITICAL)
```

**4. Аномалия (baseline deviation):**
```
IF user.login_hour NOT IN baseline_hours[user]
AND country NOT IN baseline_countries[user]
THEN alert("Impossible Travel / Unusual Login", severity=MEDIUM)
```

#### Временные окна
Корреляционные правила работают со скользящими окнами. Пример:
- Окно 5 минут: считать события с rolling window (обновляется каждую секунду)
- Окно 1 час: агрегация за каждый час (hourly bucket)

### 2.6 Алертинг

Алерт — уведомление аналитика о срабатывании правила.

Параметры алерта:
- **Rule Name / Message** — описание что произошло
- **Severity** — критичность: Critical / High / Medium / Low / Informational
- **Affected Asset** — хост, пользователь, IP
- **Evidence** — какие события привели к срабатыванию
- **Timestamp** — когда произошло

Каналы доставки алертов:
- Email (только для Medium/Low, иначе спам)
- Slack/Teams webhook (High)
- PagerDuty / OpsGenie (Critical — будит дежурного)
- Ticketing system (Jira, TheHive, ServiceNow) — создание тикета
- SOAR (Security Orchestration, Automation and Response) — автоматический ответ

### 2.7 Дашборды

Дашборды — визуализация состояния безопасности в реальном времени.

Типовые дашборды SOC L1:
- **Overview:** количество алертов по severity за 24 часа
- **Authentication:** топ неудачных логинов по IP и пользователю
- **Network:** топ источников трафика, аномалии
- **Endpoints:** хосты с малвером, подозрительные процессы
- **Geolocation:** карта мира с источниками атак

---

## 3. Популярные SIEM-решения

### 3.1 Splunk

**Тип:** Коммерческий (есть бесплатная версия до 500 МБ/день)
**Язык запросов:** SPL (Search Processing Language)
**Преимущества:**
- Лидер рынка по функциональности и экосистеме
- Splunk App Store (тысячи готовых приложений)
- Великолепные дашборды "из коробки"
- Enterprise Security (ES) — готовый SIEM поверх Splunk
- SOAR через Splunk SOAR (Phantom)

**Недостатки:**
- Очень дорогой (лицензия по объёму данных — $150/ГБ/год)
- Ресурсоёмкий

**Когда использовать:** Enterprise-среда с бюджетом, SOC-сертификация Splunk Core Certified Power User ценится.

### 3.2 ELK Stack (Elastic SIEM)

**Тип:** Open-source (Elasticsearch, Logstash, Kibana)
**Язык запросов:** KQL (Kibana Query Language), ES Query DSL, Lucene
**Преимущества:**
- Бесплатный (базовая лицензия)
- Огромное сообщество, документация
- Elastic Security — готовый SIEM с детекционными правилами
- Хорошо масштабируется

**Недостатки:**
- Требует значительной настройки
- Сложнее в обслуживании чем Splunk
- ML-функции (аномалии) — только в платной версии

**Когда использовать:** При ограниченном бюджете, когда есть DevOps-компетенции.

### 3.3 Wazuh

**Тип:** Open-source (форк OSSEC)
**Репозиторий:** github.com/wazuh/wazuh
**Преимущества:**
- Абсолютно бесплатный
- Встроенный HIDS + SIEM в одном решении
- Маппинг на MITRE ATT&CK из коробки
- Проверки соответствия: PCI DSS, GDPR, HIPAA, CIS Benchmarks
- Отличная документация
- Активный ответ: автоматическая блокировка при инциденте
- Идеален для обучения и домашних лабораторий

**Компоненты:**
- Wazuh Manager — сервер корреляции и правил
- Wazuh Agent — агент на хостах
- Wazuh Indexer — Elasticsearch-совместимое хранилище
- Wazuh Dashboard — Kibana-совместимый интерфейс

**Когда использовать:** Обучение, домашняя лаба, SMB без бюджета на коммерческий SIEM.

### 3.4 IBM QRadar

**Тип:** Коммерческий (есть Community Edition бесплатно)
**Преимущества:**
- Сильная корреляция, хорошая встроенная аналитика
- QRadar Advisor with Watson (AI-анализ)
- Популярен в крупных европейских и американских компаниях

**Недостатки:**
- Дорогой, сложный в администрировании
- Сертификация QRadar SIEM (CSFA) ценится на рынке труда

### 3.5 KUMA (Kaspersky)

**Тип:** Коммерческий, российская разработка
**Производитель:** Лаборатория Касперского
**Особенности:**
- Ориентирован на российский рынок (интеграция с российскими источниками данных)
- Соответствие требованиям ФСБ, ФСТЭК
- Работает на российском стеке (может работать без иностранных компонентов)
- Интеграция с Kaspersky TIP (Threat Intelligence Platform)

**Когда использовать:** Организации, работающие по российским требованиям ИБ, КИИ.

### 3.6 MaxPatrol SIEM (Positive Technologies)

**Тип:** Коммерческий, российская разработка
**Производитель:** Positive Technologies
**Особенности:**
- Интеграция с MaxPatrol 8 (сканер уязвимостей) и PT NAD (анализ трафика)
- Активная разработка для требований российских регуляторов
- Экспертные пакеты для обнаружения атак на АСУ ТП, AD

**Когда использовать:** Российские организации с требованиями регуляторов, КИИ, крупный enterprise.

---

## 4. Ключевые метрики SIEM

### EPS (Events Per Second)

EPS — количество событий, обрабатываемых SIEM в секунду. Ключевая метрика для лицензирования и планирования мощностей.

```
Малое предприятие (до 100 хостов): 100–500 EPS
Средний бизнес (100–1000 хостов): 500–5000 EPS
Крупный enterprise (1000+ хостов): 5000–50000+ EPS
```

Расчёт:
- 1 хост Windows с Sysmon ≈ 5–20 EPS
- 1 файрвол периметра ≈ 100–1000 EPS
- 1 веб-сервер с высокой нагрузкой ≈ 50–500 EPS

### Retention (срок хранения данных)

Сколько дней хранятся "горячие" (доступные для поиска) данные. Баланс между:
- Возможностью расследования исторических инцидентов
- Стоимостью хранения (особенно в Splunk)

### MTTD (Mean Time To Detect)

Среднее время от начала инцидента до его обнаружения. Цель SOC — снижать MTTD.
- Мировая медиана (по данным IBM X-Force, 2023): **204 дня** без SIEM
- С хорошим SIEM и настроенными правилами: **часы или дни**

### MTTR (Mean Time To Respond)

Среднее время от обнаружения до завершения реагирования. Включает:
- Время триажа алерта
- Время расследования
- Время containment и eradication
- Время recovery

Цель: сократить MTTR через автоматизацию (SOAR) и плейбуки.

---

## 5. Домашняя SOC-лаборатория: архитектура

Минимальная лаборатория для обучения:

```
┌──────────────────────────────────────────────────────────┐
│                    HOST MACHINE                          │
│  (8+ GB RAM, 4+ CPU cores, 100+ GB disk)                │
│                                                          │
│  ┌─────────────────────────────────────────────────┐    │
│  │              VirtualBox / VMware                │    │
│  │                                                 │    │
│  │  ┌──────────────────┐   ┌────────────────────┐ │    │
│  │  │  Wazuh Server    │   │  Windows 10 Agent  │ │    │
│  │  │  Ubuntu 22.04    │   │  (eval edition)    │ │    │
│  │  │  4 GB RAM        │   │  2 GB RAM          │ │    │
│  │  │  All-in-One:     │   │  Wazuh Agent       │ │    │
│  │  │  - Manager       │   │  Sysmon            │ │    │
│  │  │  - Indexer       │   └────────────────────┘ │    │
│  │  │  - Dashboard     │                           │    │
│  │  │  Port: 1514/1515 │   ┌────────────────────┐ │    │
│  │  │  Dashboard: 443  │   │  Kali Linux Agent  │ │    │
│  │  └──────────────────┘   │  (атакующая машина)│ │    │
│  │                         │  2 GB RAM           │ │    │
│  │      Internal Network   │  Wazuh Agent        │ │    │
│  │      192.168.56.0/24    └────────────────────┘ │    │
│  └─────────────────────────────────────────────────┘    │
└──────────────────────────────────────────────────────────┘
```

**Минимальные требования хост-машины:**
- RAM: 8 GB (рекомендуется 16 GB)
- CPU: 4 ядра (рекомендуется 8)
- Диск: 100 GB SSD
- ОС: Windows 10/11 или Linux

---

## 💻 Практика: Установка Wazuh All-in-One

### Шаг 1: Подготовка Ubuntu Server

Создайте виртуальную машину:
- ОС: Ubuntu Server 22.04 LTS
- RAM: 4 GB (минимум), 6 GB (рекомендуется)
- Диск: 50 GB
- Сеть: Host-Only или Internal Network (для безопасности)

После установки Ubuntu обновите систему:
```bash
sudo apt-get update && sudo apt-get upgrade -y
```

### Шаг 2: Скачивание Wazuh Installer

Wazuh предоставляет удобный установщик "всё-в-одном":
```bash
curl -sO https://packages.wazuh.com/4.7/wazuh-install.sh
curl -sO https://packages.wazuh.com/4.7/config.yml
```

### Шаг 3: Настройка config.yml

Отредактируйте файл конфигурации:
```bash
nano config.yml
```

Минимальная конфигурация для All-in-One:
```yaml
nodes:
  # Wazuh indexer nodes
  indexer:
    - name: node-1
      ip: "<WAZUH_SERVER_IP>"

  # Wazuh server nodes
  server:
    - name: wazuh-1
      ip: "<WAZUH_SERVER_IP>"

  # Wazuh dashboard nodes
  dashboard:
    - name: dashboard
      ip: "<WAZUH_SERVER_IP>"
```

Замените `<WAZUH_SERVER_IP>` на IP-адрес вашей виртуальной машины:
```bash
# Узнать IP адрес
ip addr show | grep "inet " | grep -v 127.0.0.1
```

### Шаг 4: Генерация сертификатов

```bash
bash wazuh-install.sh --generate-config-files
```

Эта команда создаст `wazuh-install-files.tar` с сертификатами и конфигурацией.

### Шаг 5: Установка Wazuh Indexer

```bash
bash wazuh-install.sh --wazuh-indexer node-1
```

Процесс занимает 3–5 минут. После завершения:
```bash
# Проверить статус
sudo systemctl status wazuh-indexer

# Инициализировать кластер
bash wazuh-install.sh --start-cluster
```

### Шаг 6: Установка Wazuh Manager

```bash
bash wazuh-install.sh --wazuh-server wazuh-1
```

Проверка:
```bash
sudo systemctl status wazuh-manager

# Проверить версию
sudo /var/ossec/bin/wazuh-control info
```

### Шаг 7: Установка Wazuh Dashboard

```bash
bash wazuh-install.sh --wazuh-dashboard dashboard
```

По завершении установки вы увидите сообщение с учётными данными:
```
INFO: --- Summary ---
INFO: You can access the web interface https://<WAZUH_SERVER_IP>
    User: admin
    Password: <AUTO_GENERATED_PASSWORD>
```

**Сохраните пароль!** Также его можно посмотреть в:
```bash
# Пароли хранятся в файле
sudo tar -O -xf wazuh-passwords.tar wazuh-passwords.txt
```

### Шаг 8: Первый вход в Dashboard

1. Откройте браузер: `https://<IP_адрес_Wazuh>`
2. Примите самоподписанный сертификат
3. Войдите: admin / `<пароль из шага 7>`

Вы увидите Wazuh Dashboard с:
- Overview: статус агентов
- Security Events: последние алерты
- MITRE ATT&CK: маппинг обнаружений

### Шаг 9: Подключение первого агента (Linux)

На агентской машине (Ubuntu/Debian):
```bash
# Добавить репозиторий Wazuh
curl -s https://packages.wazuh.com/key/GPG-KEY-WAZUH | sudo gpg --dearmor -o /usr/share/keyrings/wazuh.gpg
echo "deb [signed-by=/usr/share/keyrings/wazuh.gpg] https://packages.wazuh.com/4.x/apt/ stable main" | sudo tee /etc/apt/sources.list.d/wazuh.list

sudo apt-get update

# Установить агент
sudo WAZUH_MANAGER="<IP_WAZUH_SERVER>" apt-get install wazuh-agent -y

# Запустить агент
sudo systemctl daemon-reload
sudo systemctl enable wazuh-agent
sudo systemctl start wazuh-agent

# Проверить статус
sudo systemctl status wazuh-agent
```

### Шаг 10: Проверка работы

На Wazuh Dashboard:
1. Перейдите: Agents → Agent List
2. Убедитесь, что агент имеет статус **Active**
3. Перейдите в Security Events — должны появиться события с агента

Проверка через CLI (на Wazuh Manager):
```bash
# Список агентов
sudo /var/ossec/bin/agent_control -l

# Статистика менеджера
sudo /var/ossec/bin/wazuh-control status
```

---

## 🛠️ Задания

### ⭐ Задание 1: Исследование компонентов SIEM

Нарисуйте (на бумаге или в draw.io/Excalidraw) схему архитектуры SIEM для гипотетической компании из 200 сотрудников с:
- 150 рабочих станций Windows
- 20 серверов Linux
- 1 файрвол Cisco ASA
- 1 прокси-сервер Squid
- 1 почтовый шлюз

На схеме отразите: источники → агенты → нормализация → хранение → корреляция → алертинг → аналитик.

Рассчитайте примерное количество EPS (используя значения из теории).

### ⭐⭐ Задание 2: Развёртывание Wazuh и настройка агентов

1. Разверните Wazuh All-in-One согласно практике главы
2. Подключите минимум 2 агента (Linux и/или Windows)
3. Убедитесь, что агенты отображаются как Active в Dashboard
4. Найдите первые 10 алертов и опишите их:
   - Что сработало?
   - Какой rule_id и description?
   - Какой MITRE ATT&CK tactic/technique?
   - True Positive или False Positive?

### ⭐⭐⭐ Задание 3: Стресс-тест и анализ

1. Установите Sysmon на агент Windows с конфигурацией SwiftOnSecurity Sysmon Config
2. Выполните с агентской машины несколько тестовых активностей:
   - 5 неудачных попыток входа через SSH/RDP
   - Запуск нескольких команд PowerShell (Get-Process, whoami, net user)
   - Скачивание файла через curl/wget
3. Найдите эти события в Wazuh Dashboard
4. Определите, какие правила Wazuh сработали
5. Напишите краткий отчёт: что было обнаружено, какие правила сработали, какие активности НЕ попали в алерты (почему?)

---

## ✅ Чеклист готовности

- [ ] Я могу объяснить, что такое SIEM и почему он нужен в корпоративной среде
- [ ] Я понимаю все компоненты архитектуры SIEM (источники → агенты → нормализация → хранение → корреляция → алертинг → дашборды)
- [ ] Я знаю ключевые источники данных: Windows Event Log (с важными Event ID), Syslog, Firewall, IDS/IPS
- [ ] Я понимаю разницу между форматами CEF, LEEF, ECS
- [ ] Я могу назвать 5+ популярных SIEM-решений и их особенности
- [ ] Я знаю метрики: EPS, retention, MTTD, MTTR
- [ ] Я успешно развернул Wazuh All-in-One
- [ ] Я подключил хотя бы один агент и вижу события в Dashboard

---

## 🔗 Ресурсы

**Официальная документация:**
- [Wazuh Documentation](https://documentation.wazuh.com) — полная документация, туториалы
- [Wazuh Quick Start](https://documentation.wazuh.com/current/quickstart.html) — быстрая установка
- [Elastic SIEM](https://www.elastic.co/security) — официальный сайт

**Обучение:**
- [Wazuh Training](https://wazuh.com/training/) — бесплатные курсы от создателей
- [TryHackMe — Wazuh Room](https://tryhackme.com/room/wazuhct) — практическая лаборатория
- [TryHackMe — SIEM Room](https://tryhackme.com/room/introtosiem) — введение в SIEM

**Статьи и книги:**
- NIST SP 800-92 "Guide to Computer Security Log Management" — бесплатно на nvlpubs.nist.gov
- "The Practice of Network Security Monitoring" by Richard Bejtlich
- [Уловки и ошибки при настройке SIEM](https://habr.com/ru/companies/pt/articles/) — Positive Technologies на Habr

**Дашборды и правила:**
- [Wazuh Detection Rules](https://github.com/wazuh/wazuh-ruleset) — официальный репозиторий правил
- [Elastic Detection Rules](https://github.com/elastic/detection-rules) — правила Elastic Security
- [Sigma Rules](https://github.com/SigmaHQ/sigma) — кроссплатформенные правила детекции

**Инструменты:**
- [draw.io](https://draw.io) — бесплатный инструмент для сетевых схем
- [Wazuh Virtual Appliance](https://documentation.wazuh.com/current/deployment-options/virtual-machine/virtual-machine.html) — готовый OVA-образ для VirtualBox
