# Глава 13.3: CVSS-оценка и классификация рисков

## 🎯 Цели главы

- Разобраться в системе CVSS: версии 2, 3.0, 3.1 и 4.0
- Освоить все 8 базовых метрик CVSS v3.1 и их значения
- Научиться рассчитывать CVSS Score и интерпретировать результат
- Понять Temporal и Environmental метрики
- Изучить CWE и его связь с CVE и CVSS
- Применять OWASP Risk Rating Methodology
- Строить собственную матрицу рисков
- Рассчитать CVSS для 10 реальных уязвимостей

---

## 1. Что такое CVSS

CVSS (Common Vulnerability Scoring System) — это открытый стандарт оценки серьёзности уязвимостей в программном обеспечении. Разработан организацией FIRST (Forum of Incident Response and Security Teams) и является индустриальным стандартом, используемым в NVD (National Vulnerability Database), CERT, вендорами и пентестерами по всему миру.

### 1.1 Эволюция версий

| Версия | Год | Ключевые изменения |
|--------|-----|--------------------|
| CVSSv1 | 2005 | Первая версия, базовые метрики |
| CVSSv2 | 2007 | Temporal и Environmental метрики, широкое принятие |
| CVSSv3.0 | 2015 | Новые метрики Scope, PR, UI; пересмотр формулы |
| CVSSv3.1 | 2019 | Уточнение формулировок, без изменения формулы |
| CVSSv4.0 | 2023 | Threat метрики, новые уровни детализации |

### 1.2 Зачем CVSS пентестеру

```
Без CVSS:
"Мы нашли SQL Injection. Это плохо."

С CVSS:
"SQL Injection в эндпоинте /login (CVSS:3.1/AV:N/AC:L/PR:N/UI:N/S:U/C:H/I:H/A:H = 9.8 Critical).
Атака выполняется через интернет без аутентификации, даёт полный доступ к БД."
```

CVSS обеспечивает:
- **Объективность**: стандартизированная метрика, не зависящая от субъективной оценки
- **Сравнимость**: возможность сравнить уязвимости между собой
- **Приоритизацию**: что исправлять в первую очередь
- **Коммуникацию**: общий язык между безопасниками, разработчиками и менеджментом
- **Соответствие**: требования PCI DSS, ISO 27001 ссылаются на CVSS

:::warning
CVSS измеряет техническую серьёзность уязвимости, но НЕ учитывает контекст бизнеса. CVSS 9.8 для уязвимости в изолированной системе может быть менее критично, чем CVSS 6.5 для уязвимости в публичном API, обрабатывающем платёжные данные. Всегда добавляйте бизнес-контекст.
:::

---

## 2. CVSS v3.1: Base Score

### 2.1 Восемь базовых метрик

```
CVSS v3.1 Base Score
├── Exploitability Metrics (Метрики эксплуатируемости)
│   ├── Attack Vector (AV)
│   ├── Attack Complexity (AC)
│   ├── Privileges Required (PR)
│   └── User Interaction (UI)
├── Scope (S)
└── Impact Metrics (Метрики воздействия)
    ├── Confidentiality Impact (C)
    ├── Integrity Impact (I)
    └── Availability Impact (A)
```

### 2.2 Attack Vector (AV) — Вектор атаки

Определяет, откуда может быть проведена атака.

| Значение | Код | Числовое | Описание |
|----------|-----|----------|----------|
| Network | N | 0.85 | Атака через интернет/сеть, без физического доступа |
| Adjacent | A | 0.62 | Атака из локальной сети/Bluetooth/802.11 |
| Local | L | 0.55 | Атака требует локального доступа или shell |
| Physical | P | 0.20 | Атака требует физического контакта с устройством |

**Примеры:**

```
AV:Network  — SQL Injection в веб-приложении (через HTTP из интернета)
AV:Adjacent — Уязвимость в Wi-Fi протоколе (атакующий в той же сети)
AV:Local    — Privilege Escalation через SUID-бинарный файл (нужен shell)
AV:Physical — Evil Maid Attack (физический доступ к машине)
```

### 2.3 Attack Complexity (AC) — Сложность атаки

Отражает условия, которые не контролирует атакующий.

| Значение | Код | Числовое | Описание |
|----------|-----|----------|----------|
| Low | L | 0.77 | Атака воспроизводима в любое время, нет специальных условий |
| High | H | 0.44 | Требуются специальные условия (race condition, конкретная конфигурация) |

**Примеры:**

```
AC:Low  — SQL Injection: просто отправь запрос с payload
AC:High — Race Condition: нужно точное совпадение по времени
AC:High — Атака через hash collision (требует предварительных вычислений)
```

:::tip
AC:High НЕ означает "нужны навыки". Это про внешние условия. Сложная техническая атака, которая воспроизводится всегда — это AC:Low.
:::

### 2.4 Privileges Required (PR) — Требуемые привилегии

| Значение | Код | Числовое (S:U) | Числовое (S:C) | Описание |
|----------|-----|----------------|----------------|----------|
| None | N | 0.85 | 0.85 | Аутентификация не нужна |
| Low | L | 0.62 | 0.68 | Нужна обычная учётная запись |
| High | H | 0.27 | 0.50 | Нужна административная учётная запись |

**Примеры:**

```
PR:None — SQL Injection в публичной форме входа
PR:Low  — IDOR: обычный пользователь читает чужие данные
PR:High — Privilege Escalation из администратора в root
```

### 2.5 User Interaction (UI) — Взаимодействие пользователя

| Значение | Код | Числовое | Описание |
|----------|-----|----------|----------|
| None | N | 0.85 | Жертва не должна ничего делать |
| Required | R | 0.62 | Жертва должна выполнить действие (кликнуть ссылку, открыть файл) |

**Примеры:**

```
UI:None     — Stored XSS (payload уже в БД, выполняется при загрузке)
UI:Required — Reflected XSS (нужно, чтобы жертва кликнула по специальной ссылке)
UI:Required — Malicious PDF (жертва должна открыть файл)
```

### 2.6 Scope (S) — Область воздействия

Ключевая метрика, введённая в CVSSv3. Определяет, выходит ли уязвимость за пределы уязвимого компонента.

| Значение | Код | Описание |
|----------|-----|----------|
| Unchanged | U | Уязвимость влияет только на сам уязвимый компонент |
| Changed | C | Уязвимость позволяет воздействовать на другие компоненты |

**Примеры:**

```
S:Unchanged — SQL Injection в веб-приложении (влияет на БД того же приложения)
S:Changed   — XSS (браузер жертвы — отдельный компонент от уязвимого сервера)
S:Changed   — Sandbox Escape (выход из виртуальной машины в хост)
S:Changed   — Container Escape (выход из Docker в хост-систему)
```

:::warning
Scope Changed влияет на формулу расчёта — увеличивает итоговый score. XSS часто получает S:Changed, что повышает оценку.
:::

### 2.7 Confidentiality, Integrity, Availability Impact (C/I/A)

Три метрики воздействия на классическую триаду CIA:

| Значение | Код | Числовое | Описание |
|----------|-----|----------|----------|
| None | N | 0.00 | Нет воздействия |
| Low | L | 0.22 | Частичное воздействие, не на все данные/функции |
| High | H | 0.56 | Полное воздействие |

**Конфиденциальность (C):**
```
C:None — Уязвимость не раскрывает данные
C:Low  — Утечка некритичных данных (публичная информация)
C:High — Полная утечка: все данные, пароли, приватные ключи
```

**Целостность (I):**
```
I:None — Данные нельзя изменить
I:Low  — Можно изменить некоторые данные, ограниченно
I:High — Можно изменить любые данные или выполнить произвольный код
```

**Доступность (A):**
```
A:None — Сервис не нарушается
A:Low  — Снижение производительности, не полный отказ
A:High — Полный отказ сервиса (DoS)
```

---

## 3. Расчёт CVSS v3.1

### 3.1 Формулы

```python
import math

def roundup(x):
    """Округление вверх до одного знака после запятой"""
    return math.ceil(x * 10) / 10

def cvss_v31(AV, AC, PR, UI, S, C, I, A):
    """
    Расчёт CVSS v3.1 Base Score
    
    Все параметры передаются как числовые значения из таблицы весов.
    S = 'U' (Unchanged) или 'C' (Changed)
    """
    
    # ISS (Impact Sub-Score)
    ISS = 1 - (1 - C) * (1 - I) * (1 - A)
    
    # Scope модифицирует Impact
    if S == 'U':
        Impact = 6.42 * ISS
    else:  # S == 'C'
        Impact = 7.52 * (ISS - 0.029) - 3.25 * pow((ISS - 0.02), 15)
    
    # Exploitability Sub-Score
    Exploitability = 8.22 * AV * AC * PR * UI
    
    # Если Impact = 0, Score = 0
    if Impact <= 0:
        return 0.0
    
    # Финальный расчёт
    if S == 'U':
        score = min(Impact + Exploitability, 10)
    else:  # S == 'C'
        score = min(1.08 * (Impact + Exploitability), 10)
    
    return roundup(score)


# Таблицы весов
WEIGHTS = {
    'AV': {'N': 0.85, 'A': 0.62, 'L': 0.55, 'P': 0.20},
    'AC': {'L': 0.77, 'H': 0.44},
    'PR': {
        'U': {'N': 0.85, 'L': 0.62, 'H': 0.27},  # Scope: Unchanged
        'C': {'N': 0.85, 'L': 0.68, 'H': 0.50},  # Scope: Changed
    },
    'UI': {'N': 0.85, 'R': 0.62},
    'CIA': {'N': 0.00, 'L': 0.22, 'H': 0.56},
}


# Пример: SQL Injection (Critical)
score = cvss_v31(
    AV=WEIGHTS['AV']['N'],    # Network
    AC=WEIGHTS['AC']['L'],    # Low
    PR=WEIGHTS['PR']['U']['N'],  # None (Scope: Unchanged)
    UI=WEIGHTS['UI']['N'],    # None
    S='U',                    # Unchanged
    C=WEIGHTS['CIA']['H'],    # High
    I=WEIGHTS['CIA']['H'],    # High
    A=WEIGHTS['CIA']['H']     # High
)
print(f"SQL Injection CVSS: {score}")
# Вывод: SQL Injection CVSS: 9.8


# Пример: Reflected XSS (High)
score_xss = cvss_v31(
    AV=WEIGHTS['AV']['N'],    # Network
    AC=WEIGHTS['AC']['L'],    # Low
    PR=WEIGHTS['PR']['C']['N'],  # None (Scope: Changed)
    UI=WEIGHTS['UI']['R'],    # Required
    S='C',                    # Changed (браузер жертвы)
    C=WEIGHTS['CIA']['L'],    # Low
    I=WEIGHTS['CIA']['L'],    # Low
    A=WEIGHTS['CIA']['N']     # None
)
print(f"Reflected XSS CVSS: {score_xss}")
# Вывод: Reflected XSS CVSS: 6.1
```

### 3.2 NVD CVSS Calculator

Онлайн-калькулятор: https://nvd.nist.gov/vuln-metrics/cvss/v3-calculator

```
Шаг 1: Выберите версию CVSS 3.1
Шаг 2: Заполните каждую метрику
Шаг 3: Система автоматически рассчитает Score и вектор

Результат:
- Score: 9.8
- Vector: CVSS:3.1/AV:N/AC:L/PR:N/UI:N/S:U/C:H/I:H/A:H
- Severity: Critical
```

### 3.3 Интерпретация результата

| Диапазон | Severity | Цвет | Действие |
|----------|----------|------|---------|
| 0.0 | None | ⬜ | Информационная |
| 0.1 – 3.9 | Low | 🟢 | Исправить в плановом порядке |
| 4.0 – 6.9 | Medium | 🟡 | Исправить в ближайший спринт |
| 7.0 – 8.9 | High | 🟠 | Исправить в течение 1–2 недель |
| 9.0 – 10.0 | Critical | 🔴 | Исправить немедленно (24–72 ч) |

---

## 4. Temporal Metrics (Временные метрики)

Temporal метрики учитывают изменение риска со временем — появление публичного эксплоита, исправления и т.д.

### 4.1 Exploit Code Maturity (E) — Зрелость эксплоита

| Значение | Код | Описание |
|----------|-----|----------|
| Not Defined | X | Не учитывается |
| Unproven | U | Нет публичного эксплоита, только теория |
| Proof-of-Concept | P | Есть PoC, но не "weaponized" |
| Functional | F | Рабочий эксплоит существует |
| High | H | Атаки в дикой природе (in the wild) |

### 4.2 Remediation Level (RL) — Уровень исправления

| Значение | Код | Описание |
|----------|-----|----------|
| Not Defined | X | Не учитывается |
| Official Fix | O | Официальный патч выпущен |
| Temporary Fix | T | Есть временное исправление/workaround |
| Workaround | W | Есть обходное решение |
| Unavailable | U | Исправления нет |

### 4.3 Report Confidence (RC) — Достоверность отчёта

| Значение | Код | Описание |
|----------|-----|----------|
| Not Defined | X | Не учитывается |
| Unknown | U | Уязвимость не подтверждена |
| Reasonable | R | Есть косвенные доказательства |
| Confirmed | C | Уязвимость подтверждена вендором/исследователем |

**Пример Temporal Score:**

```
Base Score: 9.8 (CVSS:3.1/AV:N/AC:L/PR:N/UI:N/S:U/C:H/I:H/A:H)

Temporal: E:P/RL:O/RC:C
- E:P  — Есть PoC (повышает риск)
- RL:O — Официальный патч уже есть (снижает риск)
- RC:C — Подтверждено вендором

Temporal Score: 8.7 (немного ниже Base из-за наличия патча)
```

---

## 5. Environmental Metrics (Контекстные метрики)

Environmental метрики позволяют адаптировать CVSS Score к конкретной организации.

### 5.1 Security Requirements

Организация может указать, насколько важна для неё каждая составляющая CIA:

| Метрика | Описание |
|---------|----------|
| CR (Confidentiality Requirement) | Важность конфиденциальности для этого актива |
| IR (Integrity Requirement) | Важность целостности |
| AR (Availability Requirement) | Важность доступности |

Значения: Not Defined (X) / Low (L) / Medium (M) / High (H)

**Пример адаптации:**

```
Сценарий: SQL Injection в системе видеонаблюдения (не обрабатывает PII)

Base Score: 9.8 (C:H/I:H/A:H)

Environmental модификаторы:
- CR:L  — Конфиденциальность не критична (нет чувствительных данных)
- IR:M  — Целостность умеренно важна
- AR:H  — Доступность критична (камеры должны работать 24/7)

Environmental Score: 7.2 (High, не Critical)
→ Риск реальный, но менее критичный, чем для платёжной системы
```

### 5.2 Modified Base Metrics

Также можно переопределить базовые метрики с учётом среды:

```
MAV: Modified Attack Vector (если система изолирована от интернета)
Пример: AV:N → MAV:L (система доступна только из внутренней сети)

Реальная оценка: Base 9.8 → Environmental 5.5 (Medium)
```

:::tip
Environmental метрики особенно важны при работе с клиентами из регулируемых отраслей (банки, медицина, критическая инфраструктура). CVSS 7.0 для внутренней системы без интернет-доступа и без критичных данных — это не то же самое, что CVSS 7.0 для публичного API онлайн-банка.
:::

---

## 6. CVSS 4.0: Что нового

Версия 4.0 вышла в октябре 2023 года и представила существенные изменения.

### 6.1 Новая номенклатура

```
CVSS v4.0 уровни:
├── CVSS-B  (Base только)
├── CVSS-BT (Base + Threat)
├── CVSS-BE (Base + Environmental)
└── CVSS-BTE (Base + Threat + Environmental)
```

### 6.2 Замена Temporal на Threat Metrics

В v3.x: Exploit Code Maturity, Remediation Level, Report Confidence
В v4.0: только одна метрика **Exploit Maturity (E)**

| Значение | Описание |
|----------|----------|
| Not Defined | Не учитывается |
| Attacked | Атаки зафиксированы в публичных источниках |
| POC | PoC код публично доступен |
| Unreported | Нет публичных отчётов |

### 6.3 Новые метрики в v4.0

```
Supplemental Metrics (дополнительные, не влияют на Score):
├── Automatable (A)     — можно автоматизировать атаку?
├── Recovery (R)        — восстанавливается ли система после атаки?
├── Value Density (V)   — сколько ресурсов у атакующего на уязвимом компоненте?
├── Vulnerability Response Effort (RE) — усилия для реагирования
└── Provider Urgency (U) — оценка срочности от вендора
```

### 6.4 Сравнение v3.1 и v4.0

| Аспект | CVSS v3.1 | CVSS v4.0 |
|--------|-----------|-----------|
| Base метрики | 8 | 11 (+ 3 новых) |
| Временные метрики | 3 (E, RL, RC) | 1 (E) |
| Гранулярность | 3 уровня impact | Детальнее |
| Scope | Changed/Unchanged | Сохранён |
| Широта принятия | Высокая (де-факто стандарт) | Внедряется |

:::warning
По состоянию на 2024–2025 год большинство организаций и инструментов (NVD, Tenable, Qualys) всё ещё используют CVSS v3.1 как основной стандарт. Изучите v3.1 в первую очередь.
:::

---

## 7. CWE: Common Weakness Enumeration

### 7.1 Что такое CWE

CWE — иерархический словарь типов слабостей программного обеспечения, поддерживаемый MITRE Corporation. В отличие от CVE (конкретные уязвимости), CWE описывает классы проблем.

```
Иерархия CWE:
Pillar → Class → Base → Variant → Code

Пример:
CWE-20 (Pillar: Improper Input Validation)
  └─ CWE-89 (Base: SQL Injection)
       └─ CWE-564 (Variant: SQL Injection via Hibernate)
```

### 7.2 Важнейшие CWE для веб-пентестера

| CWE | Название | Примеры |
|-----|----------|---------|
| CWE-79 | Cross-site Scripting (XSS) | Reflected, Stored, DOM-based |
| CWE-89 | SQL Injection | Union-based, Blind, Error-based |
| CWE-22 | Path Traversal | ../etc/passwd |
| CWE-78 | OS Command Injection | ; ls -la |
| CWE-287 | Improper Authentication | Слабые проверки входа |
| CWE-306 | Missing Authentication | API без токена |
| CWE-352 | CSRF | Подделка запросов |
| CWE-434 | Unrestricted File Upload | Загрузка .php файла |
| CWE-502 | Deserialization | PHP/Java unserialize |
| CWE-611 | XXE | External Entity Injection |
| CWE-639 | IDOR | /users/2 вместо /users/1 |
| CWE-918 | SSRF | Запросы к внутренним ресурсам |
| CWE-1321 | Prototype Pollution | __proto__ загрязнение |

### 7.3 CWE Top 25 (2023)

MITRE ежегодно публикует список самых опасных слабостей:

```
#1  CWE-787  Out-of-bounds Write
#2  CWE-79   XSS
#3  CWE-89   SQL Injection
#4  CWE-416  Use After Free
#5  CWE-78   OS Command Injection
#6  CWE-20   Improper Input Validation
#7  CWE-125  Out-of-bounds Read
#8  CWE-22   Path Traversal
#9  CWE-352  CSRF
#10 CWE-434  Unrestricted File Upload
...
#25 CWE-94   Code Injection
```

---

## 8. CVE vs CWE vs CVSS: взаимосвязь

```
┌─────────────────────────────────────────────────────────┐
│                    ВЗАИМОСВЯЗЬ                          │
├─────────────────────────────────────────────────────────┤
│                                                         │
│  CVE-2021-44228 (Log4Shell)                            │
│  │                                                      │
│  ├── CWE: CWE-917 (Improper Neutralization in          │
│  │              Expression Language Statement)          │
│  │        ↳ Тип уязвимости (что это такое)             │
│  │                                                      │
│  └── CVSS: 10.0 Critical                               │
│           (CVSS:3.1/AV:N/AC:L/PR:N/UI:N/S:C/C:H/I:H/A:H)
│           ↳ Оценка серьёзности (насколько это опасно)  │
│                                                         │
│  CVE = Конкретная уязвимость (что сломано)             │
│  CWE = Класс уязвимости (тип проблемы)                 │
│  CVSS = Оценка серьёзности (насколько плохо)           │
│                                                         │
└─────────────────────────────────────────────────────────┘
```

**Практическое использование:**

```markdown
### В отчёте:

**F-01: SQL Injection**
- CVE: CVE-2023-XXXXX (если это известная библиотека с уязвимостью)
- CWE: CWE-89 (тип уязвимости)
- CVSS: 9.8 (CVSS:3.1/AV:N/AC:L/PR:N/UI:N/S:U/C:H/I:H/A:H) (оценка)
```

---

## 9. OWASP Risk Rating Methodology

### 9.1 Отличие от CVSS

OWASP Risk Rating — субъективная, но более гибкая методология, учитывающая бизнес-контекст с самого начала.

```
OWASP Risk Rating =
    Likelihood (Вероятность) × Impact (Воздействие)

Likelihood = (Threat Agent Factors + Vulnerability Factors) / 2
Impact = (Technical Impact + Business Impact) / 2
```

### 9.2 Факторы оценки

**Threat Agent Factors (факторы угрозы):**

| Фактор | 1 (Low) | 6 (Medium) | 9 (High) |
|--------|---------|-----------|---------|
| Навыки | Эксперт | Опытный | Начинающий |
| Мотивация | Низкая | Финансовая | Высокая |
| Возможность | Полный доступ | Частичный | Анонимный |
| Размер группы | Одиночка | Инсайдеры | Все пользователи |

**Vulnerability Factors (факторы уязвимости):**

| Фактор | 1 (Low) | 6 (Medium) | 9 (High) |
|--------|---------|-----------|---------|
| Лёгкость обнаружения | Сложно | Легко | Автоматически |
| Лёгкость эксплуатации | Сложно | Легко | Автоматически |
| Осведомлённость | Неизвестна | Известна | Общеизвестна |
| Обнаружение вторжения | Активное | Логируется | Не логируется |

**Technical Impact:**

| Фактор | Low | Medium | High |
|--------|-----|--------|------|
| Потеря конфиденциальности | Минимум данных | Важные данные | Все данные |
| Потеря целостности | Небольшое повреждение | Серьёзное | Полное |
| Потеря доступности | Минимальный сбой | Прерывания | Полный отказ |
| Подотчётность | Полностью отслеживается | Возможно отследить | Анонимно |

### 9.3 Матрица рисков OWASP

```
                    IMPACT
                Low    Medium    High
              ┌──────┬──────────┬──────────┐
         Low  │  Low │  Low     │  Medium  │
L        ├──────┼──────────┼──────────┤
I   Medium   │  Low │  Medium  │  High    │
K        ├──────┼──────────┼──────────┤
E    High    │ Medium│  High   │ Critical │
L        └──────┴──────────┴──────────┘
I
H
O
O
D
```

### 9.4 Пример расчёта по OWASP

```
Уязвимость: Reflected XSS в поиске

Likelihood:
- Threat Agent: Любой интернет-пользователь = 9
- Уязвимость: легко обнаружить, легко эксплуатировать = 7
- Likelihood = (9 + 7) / 2 = 8.0 → High

Impact:
- Техническое: кража сессии (High = 7), изменение контента (Medium = 5)
- Бизнес: репутационный ущерб (High = 7), небольшой финансовый (Low = 3)
- Impact = (7 + 5 + 7 + 3) / 4 = 5.5 → Medium

Risk = High Likelihood × Medium Impact = HIGH
```

---

## 10. Собственная матрица рисков

### 10.1 Создание матрицы для организации

Пентестеры часто создают кастомные матрицы для конкретных клиентов:

```python
# Матрица рисков 5x5
LIKELIHOOD = {
    1: "Очень низкая (раз в 10 лет)",
    2: "Низкая (раз в год)",
    3: "Средняя (несколько раз в год)",
    4: "Высокая (ежемесячно)",
    5: "Очень высокая (постоянно)"
}

IMPACT = {
    1: "Незначительное (нет видимых последствий)",
    2: "Незначительное (небольшой сбой)",
    3: "Умеренное (финансовые потери < $10K)",
    4: "Серьёзное (финансовые потери $10K-$100K)",
    5: "Катастрофическое (финансовые потери > $100K)"
}

RISK_MATRIX = {
    (1,1): "Low",   (1,2): "Low",    (1,3): "Medium", (1,4): "High",     (1,5): "Critical",
    (2,1): "Low",   (2,2): "Low",    (2,3): "Medium", (2,4): "High",     (2,5): "Critical",
    (3,1): "Low",   (3,2): "Medium", (3,3): "Medium", (3,4): "High",     (3,5): "Critical",
    (4,1): "Medium",(4,2): "Medium", (4,3): "High",   (4,4): "Critical", (4,5): "Critical",
    (5,1): "Medium",(5,2): "High",   (5,3): "High",   (5,4): "Critical", (5,5): "Critical",
}

def calculate_risk(likelihood, impact):
    return RISK_MATRIX[(likelihood, impact)]

# Пример использования
print(calculate_risk(5, 5))  # Critical — атака постоянна, ущерб катастрофический
print(calculate_risk(1, 5))  # Critical — атака редкая, но ущерб катастрофический
print(calculate_risk(3, 2))  # Medium   — умеренная вероятность, малый ущерб
```

---

## 11. Примеры расчёта CVSS для 10 реальных уязвимостей

### 11.1 SQL Injection (аутентификация)

```
Вектор: CVSS:3.1/AV:N/AC:L/PR:N/UI:N/S:U/C:H/I:H/A:H

AV:N  — Эксплуатируется через HTTP из интернета
AC:L  — Нет специальных условий, простой payload
PR:N  — Не нужна аутентификация
UI:N  — Жертва не нужна
S:U   — Уязвимость в БД, не выходит за рамки приложения
C:H   — Полная утечка всей БД
I:H   — Можно изменить/удалить данные
A:H   — DoS через тяжёлые запросы / DROP TABLE

Score: 9.8 — CRITICAL
```

### 11.2 Stored XSS

```
Вектор: CVSS:3.1/AV:N/AC:L/PR:L/UI:R/S:C/C:L/I:L/A:N

AV:N  — Через интернет
AC:L  — Простая эксплуатация
PR:L  — Нужна учётная запись для сохранения payload
UI:R  — Нужно, чтобы жертва открыла страницу с XSS
S:C   — Затрагивает браузер жертвы (другой компонент!)
C:L   — Кража cookies / частичных данных
I:L   — Модификация контента страницы
A:N   — Нет влияния на доступность

Score: 5.4 — MEDIUM
```

### 11.3 Blind SQL Injection (нет вывода данных)

```
Вектор: CVSS:3.1/AV:N/AC:L/PR:N/UI:N/S:U/C:H/I:H/A:H

Примечание: Хотя вывод данных не отображается напрямую,
time-based и boolean blind SQLi всё равно даёт доступ к данным.
Метрики не меняются, поскольку возможности атаки те же.

Score: 9.8 — CRITICAL
```

### 11.4 Reflected XSS

```
Вектор: CVSS:3.1/AV:N/AC:L/PR:N/UI:R/S:C/C:L/I:L/A:N

AV:N  — Через интернет
AC:L  — Нет специальных условий
PR:N  — Не нужна авторизация
UI:R  — НУЖНО, чтобы жертва кликнула по ссылке (отличие от Stored)
S:C   — Браузер жертвы — другой компонент
C:L   — Кража cookies
I:L   — Изменение DOM страницы
A:N   — Нет

Score: 6.1 — MEDIUM
```

### 11.5 IDOR (Insecure Direct Object Reference)

```
Сценарий: /api/orders/12345 — можно изменить ID на любой другой

Вектор: CVSS:3.1/AV:N/AC:L/PR:L/UI:N/S:U/C:H/I:N/A:N

AV:N  — Через интернет
AC:L  — Просто изменить число в URL
PR:L  — Нужна любая учётная запись
UI:N  — Жертва не нужна
S:U   — Уязвимость в том же приложении
C:H   — Полный доступ к чужим данным заказов
I:N   — Только чтение (если нет изменения)
A:N   — Нет

Score: 6.5 — MEDIUM

Если IDOR позволяет также ИЗМЕНЯТЬ чужие данные:
I:H → Score: 8.1 — HIGH
```

### 11.6 File Upload → RCE

```
Вектор: CVSS:3.1/AV:N/AC:L/PR:L/UI:N/S:U/C:H/I:H/A:H

AV:N  — Через HTTP
AC:L  — Нет специальных условий
PR:L  — Нужна учётная запись пользователя (для загрузки файла)
UI:N  — Жертва не нужна
S:U   — Сервер в рамках одного приложения
C:H   — Чтение любых файлов на сервере
I:H   — Запись, изменение файлов, кода
A:H   — Остановка сервера

Score: 8.8 — HIGH

Если загрузка доступна без авторизации (PR:N):
Score: 9.8 — CRITICAL
```

### 11.7 SSRF (Server-Side Request Forgery)

```
Вектор: CVSS:3.1/AV:N/AC:L/PR:N/UI:N/S:C/C:H/I:H/A:N

AV:N  — Через интернет
AC:L  — Нет специальных условий
PR:N  — Нет аутентификации
UI:N  — Жертва не нужна
S:C   — Сервер может атаковать внутренние ресурсы (ИЗМЕНЕНИЕ SCOPE)
C:H   — Доступ к внутренним сервисам, metadata service (AWS keys)
I:H   — Возможность взаимодействия с внутренними API
A:N   — Доступность не затронута

Score: 10.0 — CRITICAL (максимальный!)

Ограниченный SSRF (только GET, без чтения ответа):
S:U, C:L, I:N → Score: 5.0 — MEDIUM
```

### 11.8 Local File Inclusion (LFI)

```
Вектор: CVSS:3.1/AV:N/AC:L/PR:N/UI:N/S:U/C:H/I:N/A:N

AV:N  — Через интернет (/index.php?page=../../../etc/passwd)
AC:L  — Простой path traversal
PR:N  — Нет аутентификации
UI:N  — Жертва не нужна
S:U   — Остаётся в рамках системы
C:H   — Чтение любых файлов (/etc/shadow, private keys, .env)
I:N   — Только чтение
A:N   — Нет влияния на доступность

Score: 7.5 — HIGH

LFI → RCE (через log poisoning / proc/self/fd):
I:H, A:H → Score: 9.8 — CRITICAL
```

### 11.9 XXE (XML External Entity)

```
Вектор: CVSS:3.1/AV:N/AC:L/PR:N/UI:N/S:U/C:H/I:L/A:L

AV:N  — Через HTTP (XML в теле запроса)
AC:L  — Нет специальных условий
PR:N  — Публичный API
UI:N  — Жертва не нужна
S:U   — В рамках приложения
C:H   — Чтение файлов, внутренних сервисов
I:L   — Ограниченное SSRF к внутренним сервисам
A:L   — Потенциальный Billion Laughs DoS

Score: 8.1 — HIGH

Примечание: Если XXE используется только для OOB data exfiltration
(blind XXE), AC может быть H (требует внешнего сервера):
AC:H → Score: 7.4 — HIGH
```

### 11.10 Insecure Deserialization → RCE

```
Вектор: CVSS:3.1/AV:N/AC:H/PR:N/UI:N/S:U/C:H/I:H/A:H

AV:N  — Через HTTP (сериализованный объект в cookie или теле)
AC:H  — Требует понимания структуры объектов и доступных гаджетов
PR:N  — Нет аутентификации (если cookie присваивается анонимно)
UI:N  — Жертва не нужна
S:U   — Сервер в рамках приложения
C:H   — Полный RCE → чтение всего
I:H   — Полный RCE → изменение всего
A:H   — Полный RCE → остановка сервиса

Score: 8.1 — HIGH (AC:H снижает от 9.8)

Если есть готовый автоматизированный эксплоит (ysoserial, phpggc):
AC:L → Score: 9.8 — CRITICAL
```

---

## 12. Практика: Самостоятельный расчёт CVSS

### 12.1 Сценарий 1: Path Traversal в загрузке файлов

```
Описание:
Приложение принимает имя файла для скачивания через параметр URL:
GET /download?file=report.pdf

Атакующий может указать:
GET /download?file=../../../../etc/passwd

Условия:
- Приложение доступно через интернет
- Нужна авторизация (любой пользователь)
- Можно читать файлы, но не записывать
- Можно прочитать /etc/passwd, конфиги, .env файлы
- Нельзя нарушить работу сервиса
```

**Задание:** Рассчитайте CVSS v3.1 и обоснуйте каждую метрику.

```
Ответ (разверните для проверки):
AV:N  — Интернет-приложение
AC:L  — Простая манипуляция параметром
PR:L  — Нужна авторизация
UI:N  — Жертва не нужна
S:U   — В рамках файловой системы того же сервера
C:H   — Доступ к конфигам, паролям, приватным ключам
I:N   — Только чтение
A:N   — Нет влияния на доступность

Вектор: CVSS:3.1/AV:N/AC:L/PR:L/UI:N/S:U/C:H/I:N/A:N
Score: 6.5 — MEDIUM
```

### 12.2 Сценарий 2: Broken Access Control в API

```
Описание:
REST API для мобильного приложения:
GET /api/v1/users/{user_id}/profile

Авторизованный пользователь user_id=100 может обратиться к:
GET /api/v1/users/1/profile (данные администратора)
GET /api/v1/users/200/profile (данные другого пользователя)

API возвращает: имя, email, телефон, адрес, история заказов.
Изменение данных через этот endpoint невозможно.
```

**Задание:** Рассчитайте CVSS и объясните разницу с Scenario 1.

### 12.3 Сценарий 3: OS Command Injection

```
Описание:
Инструмент "ping" на веб-сайте:
POST /tools/ping
{"host": "8.8.8.8"}

Атакующий отправляет:
{"host": "8.8.8.8; cat /etc/passwd"}

Атака успешна. Сервер — не контейнер, запущен как www-data.
Нет sudo прав, но можно читать файлы www-data.
Приложение доступно анонимно.
```

**Задание:** Рассчитайте CVSS. Как изменится Score, если www-data имеет NOPASSWD sudo?

### 12.4 Сценарий 4: CSRF на смену email

```
Описание:
Форма смены email не содержит CSRF-токен:
POST /account/change-email
{"email": "new@email.com"}

Атакующий создаёт страницу, которую жертва открывает,
и её email автоматически меняется.
Для атаки нужно, чтобы жертва была авторизована и кликнула ссылку.
Жертва — обычный пользователь.
```

**Задание:** Рассчитайте CVSS. Как бы изменились метрики, если бы CSRF мог изменить пароль?

### 12.5 Сценарий 5: Слабый JWT Secret

```
Описание:
JWT-токены подписаны слабым секретом "secret123".
Инструмент hashcat разбирает его за 5 секунд.
Атакующий может сгенерировать токен с любым user_id и role.
Нет аутентификации для первоначального получения токена (регистрация открыта).
Токены используются для всего API (смена данных, просмотр, платежи).
```

**Задание:** Рассчитайте CVSS. Является ли это Critical или High? Обоснуйте.

---

## 📝 Практическое задание

### Задание 1: Калькулятор CVSS

1. Зайдите на https://nvd.nist.gov/vuln-metrics/cvss/v3-calculator
2. Рассчитайте CVSS для следующих уязвимостей:
   - Log4Shell (CVE-2021-44228) — сравните с официальным NVD
   - Spring4Shell (CVE-2022-22965)
   - Heartbleed (CVE-2014-0160)

3. Для каждой объясните, почему именно такой score:
   - Почему Log4Shell получила 10.0?
   - Почему Heartbleed 7.5, а не 10.0?

### Задание 2: Написать CVSS объяснение для клиента

Возьмите любую уязвимость из ваших лабораторных работ и напишите объяснение CVSS Score в формате:

```markdown
### Почему F-01 получил оценку 9.8 (Critical)?

Мы оцениваем эту уязвимость как Critical (9.8/10) по следующим причинам:

**Любой из интернета** (AV:Network):
[Объяснение]

**Без специальных условий** (AC:Low):
[Объяснение]

...и т.д. для каждой метрики
```

### Задание 3: Environmental Score

У вас есть SQL Injection с Base Score 9.8 в следующих сценариях:

1. Интернет-банк, БД содержит финансовые транзакции 1M клиентов
2. Внутренняя HR-система, доступна только из офисной сети
3. Тестовая среда, нет реальных данных

Рассчитайте Environmental Score для каждого сценария и обоснуйте, как изменились метрики CR/IR/AR и Modified Attack Vector.

---

## 📚 Итоги

В этой главе мы изучили:

1. **CVSS история**: от v2 до v4.0, почему v3.1 — текущий стандарт
2. **8 базовых метрик**: AV, AC, PR, UI, S, C, I, A — значения и числа
3. **Формулу расчёта**: ISS, Impact, Exploitability, округление
4. **Интерпретацию**: 0–3.9 Low, 4–6.9 Medium, 7–8.9 High, 9–10 Critical
5. **Temporal метрики**: как зрелость эксплоита меняет оценку
6. **Environmental метрики**: адаптация к конкретной организации
7. **CVSS 4.0**: новые метрики, Threat вместо Temporal
8. **CWE**: классификатор типов слабостей
9. **CVE vs CWE vs CVSS**: три разных понятия, три разных вопроса
10. **OWASP Risk Rating**: Likelihood × Impact, когда CVSS недостаточно
11. **10 примеров**: реальные расчёты с объяснением каждой метрики

### Шпаргалка по типичным CVSS Score

| Уязвимость | Типичный Score |
|------------|----------------|
| SQL Injection (auth bypass, PR:N) | 9.8 Critical |
| RCE через File Upload (PR:N) | 9.8 Critical |
| SSRF с доступом к metadata | 9.1 Critical |
| Stored XSS (PR:L) | 5.4 Medium |
| Reflected XSS (PR:N, UI:R) | 6.1 Medium |
| IDOR (только чтение) | 6.5 Medium |
| IDOR (чтение + запись) | 8.1 High |
| LFI (только чтение) | 7.5 High |
| CSRF (критичное действие) | 6.5 Medium |
| Open Redirect | 3.1–6.1 Low–Medium |

---

## Навигация

- [← Глава 13.2: Структура пентест-отчёта](./chapter-13-2.md)
- [→ Глава 14.1: Race Conditions и HTTP Request Smuggling](../part-14/chapter-14-1.md)
- [↑ Часть 13: Профессиональный пентест](../README.md)
